# 万象记 / Mosaic — UI 与操作逻辑重设计方案 v2

> 目标：在**不删减任何已实现功能**、**不改数据模型**的前提下，把交互层级和入口重新组织，
> 消除当前 UI 的繁琐感。风格取向：iOS 系统原生极简。

---

## 0. 设计原则（后续所有取舍的依据）

1. **一屏一主任务** —— 列表就是列表，不在列表行里塞一整页的内容。
2. **一个对象一个主动作** —— 点击 = 打开。其余动作一律进滑动/长按菜单。
3. **默认路径零选择** —— 用户最常做的事（写字）不应该需要先做一次选择。
4. **低频功能不占像素** —— 但必须可达，且可达路径唯一、可预期。
5. **同一件事只有一条路径** —— 现在删除一个块有 3 种方式，这本身就是繁琐。

---

## 1. 信息架构

### 1.1 改后

```
NavigationStack
├── 首页「全部笔记」                    ← 新的根页面
│   ├── 文件夹 chip 行（全部 / 未归类 / 工作 / 读书 … / ⋯）
│   ├── 笔记流：标题 · AI 一句话 · 时间
│   ├── 导航栏：左 ⚙︎设置    右 🔍搜索
│   └── 右下悬浮 ✎ 新建
│
├── 笔记页（原 CardEditorView ＋ AI 摘要贴纸，合并为一页）
├── 搜索页
├── 文件夹管理页（新，由 chip 行末尾的「⋯」进入）
└── 设置页
    └── 高级（二级页）
```

### 1.2 与现状的差异

| | 现在 | 改后 |
|---|---|---|
| 层级深度 | 3 级（文件夹 → 卡片 → 编辑器） | **2 级**（笔记流 → 笔记页） |
| `CardListView` | 文件夹详情页 | **删除**，职责并入首页 |
| `FolderListView` | 首页 | 降级为「文件夹管理页」，只管增删改排序 |
| `SummaryStickerView` | 嵌在列表行里展开 | 移入笔记页顶部 |
| 摘要与内容 | 分居两页，永不同框 | 同一页 |

---

## 2. 首页「全部笔记」

### 2.1 布局

```
┌──────────────────────────────────────┐
│ ⚙︎        全部笔记              🔍   │   导航栏（inline 标题 = 当前筛选）
├──────────────────────────────────────┤
│ (全部) 未归类  📁工作  📚读书   ⋯    │   文件夹 chip 行（横向滚动）
├──────────────────────────────────────┤
│ 📌 Q3 产品评审会                3小时前│
│    确定了排期与三位负责人，下周三上线  ● │   ← ● = 有未读更新
│                                       │
│    读《人类简史》第 4 章          昨天 │
│    围绕认知革命展开，记了 5 条要点     │
│                                       │
│    未命名笔记                     周一 │
│    今天开会讨论了…                    │   ← 无摘要时回退为正文首行
├──────────────────────────────────────┤
│                              ( ✎ )   │   悬浮新建（56pt）
└──────────────────────────────────────┘
```

### 2.2 文件夹 chip 行

- 横向滚动，单选。构成：`全部` → `未归类`（仅当存在未归类笔记时出现）→ 用户文件夹（按 `sortOrder`）→ `⋯`
- chip 内容 = 文件夹图标（用 `colorHex` 着色）+ 名称。**不显示卡片数** —— 数量对"我要看哪个文件夹"这个决策没有帮助，只占宽度。
- 选中 = 实心填充；未选中 = 淡色。再次点击已选中的 chip → 回到「全部」。
- 末尾 `⋯` chip → 文件夹管理页。
- **用户文件夹数为 0 时整行隐藏** —— 新用户第一眼看到的是干净的笔记流，不是一个空的分类系统。
- 切换 chip 时导航栏标题同步变化，用户永远知道自己在看哪个集合。

### 2.3 笔记行

只有三组信息：

```
[📌] 标题（1 行截断）                         相对时间
     AI 一句话概述（最多 2 行）                    [●]
```

- **整行是单一点击区** → 进笔记页。原来那个右侧小三角彻底删除。
- 第二行优先用 `summary.baseOneLiner`；没有摘要时回退为正文第一个文字块的**纯文本**首行（`MarkdownText.plainText`，避免 `#`/`*` 泄漏到列表里）；再没有则显示内容类型描述（如「1 段录音」）。
- `📌` 置顶标记放在标题前。
- `●` 红点 = 存在未读的更新记录（沿用 `UpdateLogEntity.isRead`）。
- **去掉**：内容类型图标行、标签 chip、chevron。这些进笔记页一眼可见，在列表里是噪音；去掉后一屏可多容纳约 40% 的笔记。
- 排序沿用 `CardSorting`：置顶优先 → `updatedAt` 倒序。

### 2.4 行手势（全局唯一的一套）

| 手势 | 动作 |
|---|---|
| 点击 | 进入笔记页 |
| 左滑 | 置顶 / 取消置顶（橙色） |
| 右滑 | 删除（红色，保留二次确认） |
| 长按 | context menu：置顶 · **移动到文件夹…** · 分享/导出… · 删除 |

> **「移动到文件夹」是本次新补的功能**。PRD §4.2 列为 P1 但一直没实现；引入「未归类」概念后它从"锦上添花"变成了必需品——否则草草记下的笔记永远出不了未归类。实现成本是改一个引用。

### 2.5 空状态

- 完全没有笔记：`还没有笔记` + `点右下角 ✎ 写下第一条，文字、录音、图片、文档、链接都能塞进来`
- 某文件夹下为空：`这个文件夹还是空的` + 一个「查看全部笔记」按钮（回到「全部」筛选）

---

## 3. 笔记页（核心页面）

### 3.1 布局

```
┌──────────────────────────────────────┐
│ ‹        📁 工作 ▾              ⋯    │   导航栏
├──────────────────────────────────────┤
│ ✨ 确定了排期与三位负责人…        ▾  │   AI 摘要条（默认收起，44pt）
├──────────────────────────────────────┤
│ Q3 产品评审会                        │   标题（可留空）
│ #排期 #分工                          │   标签（有才显示，点击展开编辑）
│                                       │
│ 今天讨论了**排期**与分工。            │
│ - 确定下周三上线                      │   块流
│ ▶︎ ▁▃▅▂▁ 00:42                       │
│ [图片]                                │
│ ▊                                     │   ← 末尾永远有一个可写文字块
├──────────────────────────────────────┤
│  📷    🎙    📄    🔗          #     │   底部常驻工具条
└──────────────────────────────────────┘
```

### 3.2 导航栏中间：文件夹选择器

`Menu` 形式，显示当前文件夹图标 + 名称 + `▾`。展开后：`未归类` / 各文件夹 / `新建文件夹…`。

这是"编辑器里可改归属"的落点，也是「移动到文件夹」的第二个入口 —— 用户在写的过程中随手归类，而不是被迫在新建前就决定。

### 3.3 导航栏右：`⋯` 菜单（所有低频动作的唯一收口）

```
置顶 / 取消置顶
────────────────
立即更新总结
重新生成完整总结
清除 AI 摘要
────────────────
导出为 Markdown
导出为纯文本
────────────────
删除笔记                    （红色）
```

这些动作现在散落在 **5 处**：顶部 pin 按钮、顶部 EditButton、底部 share 菜单、摘要贴纸里的三个按钮、以及两个 `confirmationDialog`。合并后导航栏只剩「‹ 返回 · 文件夹 ▾ · ⋯」三个元素。

### 3.4 AI 摘要区

#### 收起态（默认，一条 44pt 横条）

| 状态 | 显示 |
|---|---|
| 有摘要 | `✨ {一句话概述}` 单行截断 + `▾` |
| 有未读更新 | 同上，左侧加红点 |
| 无摘要 · 有内容 | `✨ 退出后将自动生成总结` 灰色，**不可点**，纯告知 |
| 无摘要 · 内容为空 | 整条隐藏 |
| 生成中 | `✨ AI 正在总结…` + 小转圈 |
| 失败 | `⚠︎ {错误信息}` + `重试` (+ `去设置`，仅配置类错误) |

#### 展开态（原地展开，不跳页）

```
初始总结                    kimi-k2.6 · 08-06 14:30
[会议记录]   #排期  #分工  #上线
• 确定下周三上线
• 新增三位负责人
围绕 Q3 排期展开，明确了…（80–150 字总结段）

更新记录 (3)                                     ▾
  ┗ 最新一条直接展开，其余折叠
```

**相比现在的简化：**

| 原来 | 改为 |
|---|---|
| 「立即更新」按钮常驻 | 删除按钮形态 → 退出即自动更新；强制更新移入 `⋯` |
| 「重新生成」「清除」两个按钮常驻 | 移入 `⋯` |
| 「把主题加为标签」独立按钮 | **删除按钮**。主题 chip 直接可点，点一下即加为标签并变实心。一次一个，即时反馈。 |
| 模型/时间独占一行 | 与「初始总结」标题同行右对齐 |
| 更新记录全部平铺 | ≥2 条时二级折叠，只展开最新一条 |

结果：展开态从「6 个可点控件 + 3 个弹窗」降到「主题 chips + 1 个折叠器」。

### 3.5 正文输入（备忘录式）

- **进入即可写**：卡片为空时自动创建一个空文字块并聚焦，光标就位。卡片非空时不自动聚焦（避免误改已有内容）。
- **末尾永远有一个可写文字块**：删掉最后一个块时自动补一个空块。因此「添加文字」这个动作**在概念上被移除了**。
- 空文字块在退出时自动清理（`Block.isEffectivelyEmpty` 已有），不留脏数据。

### 3.6 底部工具条（取代「＋ 添加内容」菜单）

常驻在键盘上方 / 安全区上方，一排 4 个插入图标 + 1 个标签入口：

| 图标 | 动作 |
|---|---|
| 📷 | 点击 → `Menu`：拍照 / 从相册选。相机不可用时直接进相册（不弹菜单） |
| 🎙 | 打开录音 sheet |
| 📄 | 文档选择器 |
| 🔗 | **剪贴板是 URL 时直接插入**并显示「已粘贴 example.com · 撤销」；否则弹输入框 |
| `#` | 展开 / 收起标签编辑 |

- **插入位置 = 当前光标所在块之后**；无焦点时追加到末尾。
- 插入后自动在其下方补一个空文字块并聚焦 → 「插图 → 继续写」不中断。
- 相机是唯一保留二级菜单的（真有两个来源），其余全是一次点击。

### 3.7 标签

- 编辑器里**不再有常驻的「标签」Section**。
- 已有标签：标题下方一行只读 chip，点击 → 展开编辑（复用现有 `TagEditorView`）。
- 无标签：完全不显示，靠底部 `#` 按钮召唤。

### 3.8 各内容块的简化

| 块 | 现在 | 改为 |
|---|---|---|
| **文字** | 每块右下角一个「编辑/预览」切换按钮 | **零按钮**：失焦即渲染 Markdown，聚焦即显示源码。Markdown 工具条只在该块聚焦时出现在键盘上方（复用 `RichMarkdownEditor` 的 inputAccessoryView） |
| **图片** | 缩略图 + 常驻「添加图片说明」输入框（多数时候是空的） | 缩略图；**有说明才显示说明文字**。点击 → 全屏。长按 → 添加/编辑说明 · 删除 |
| **链接** | 链接卡片 **+ 下方常驻「编辑网址」输入框**（同一信息两份） | 只有链接卡片。点击 → 应用内浏览器。长按 → 用 Safari 打开 · 复制链接 · 编辑网址 · 删除 |
| **录音** | 播放条 + DisclosureGroup 折叠的转写稿 + 内部「重新转写」按钮 + 错误行另一个「重试」按钮 | 播放条 + **转写文字直接显示在下方**（灰色小字，超 3 行折叠为「展开」）。转写中显示进度。失败时一行错误 + **单个**「重试」。「重新转写」移入长按菜单 |
| **文档** | 已经很干净 | 不变；长按菜单加「删除」 |

### 3.9 块的删除与排序（统一为一套）

- **删除顶部的 `EditButton`** —— 不再有"进入编辑模式"这个状态。
- 每个块 **长按 → context menu**：上移 · 下移 · 删除。
- 拖拽排序保留（List 的 `.onMove` 长按即可拖，本来就不需要 `EditButton`）。

从「swipe / EditButton 模式 / 无长按」三条不一致的路径，收敛为「长按菜单 + 拖拽」两条语义一致的路径。

### 3.10 退出行为

返回时按序执行：

1. flush autosave
2. 清理空块
3. 若 `autoUpdateSummary` 开启 && 已同意隐私 && 内容足够：
   - **无 base → 生成初始总结**
   - 有 base && 有实质变更 → 追加一条更新记录
4. 生成在后台进行，首页对应行的一句话位置显示「总结中…」

> **关键改动**：`ensureBaseIfNeeded` 从 `onAppear` 移到退出流程，与更新总结走同一个判定入口。
> 用户只需要理解**一条**规则：「写完退出，AI 就会总结」。现在的两套机制（首次展开小三角生成初始总结 / 退出时生成更新记录）合并为一套。
> 副作用是**只是点进去看一眼不会花钱**——比现在的行为更省。

---

## 4. 搜索页

- 入口从"只有文件夹列表页有"改为**首页导航栏常驻** 🔍。
- 顶部加一行**标签 chip**，承接从首页移走的标签筛选：点 chip → 把该标签填入搜索框。`SearchMatcher` 本来就支持 tag 匹配，几乎无需改逻辑。
- 结果行与首页笔记行同构，额外显示所属文件夹。
- 其余不变（250ms 防抖、`SearchScope`、`CardSearchText.haystack`）。

---

## 5. 文件夹管理页

即现在的 `FolderListView`，降级为二级页：

- 列表：图标 + 名称 + 卡片数
- 右上 `＋` 新建
- 左滑：重命名 / 删除（保留二次确认与「将同时删除其中 N 张卡片」）
- **新增拖动排序** —— PRD §4.1 标为 P1，`sortOrder` 字段早已存在，只差 UI
- 「未归类」不出现在这个列表里（它不是真文件夹，不可重命名、不可删除）

---

## 6. 设置页分层

### 第一屏（只放真正需要决策的项）

```
AI 服务商
  服务商             Kimi ▾
  API Key            ••••••••••
  [ 测试连接 ]

摘要
  内容变更后自动追加更新总结   ⬜︎

语音转写
  转写方式           Apple 本地转写 ▾
  识别语言           自动 ▾

高级                                    ›
关于                                    ›
```

### 「高级」二级页

Base URL · 模型名 · 发送图片给 AI（视觉）· JSON 输出模式 · STT 模型 / Base URL / API Key 覆盖 · 启用 iCloud 同步 · 重置隐私同意状态 · 重置音频上传同意状态

**例外**：当「服务商 = 自定义」时，`Base URL` 和 `模型名` 自动提升回第一屏 —— 此时它们不是高级选项，而是必填项。

---

## 7. 数据层影响：几乎为零

- **`Card.folder` 已经是 `Folder?`**（[Card.swift:20](App/Mosaic/Persistence/Card.swift:20)）。
  所以「未归类」直接用 `folder == nil` 表达 —— **不需要新建默认 Folder 行、不需要 migration、不需要处理"默认文件夹被用户删掉"的边界**，而且它天然不可重命名、不可删除，正是一个系统桶该有的性质。原本以为要改模型，实际上零成本。
- 首页需要一个全局 `@Query<Card>` —— `SearchView.swift:9` 里已有一模一样的写法可以照搬。
- 「移动到文件夹」= 改一个引用 + `touch()`。
- **`MosaicKit` 零改动**：`CardSorting` / `SearchMatcher` / `TagUtilities` / `MarkdownText` / `CardExportFormatter` / 所有 AI 逻辑全部原样复用，现有 119 条断言继续有效。

---

## 8. 功能保全对照表

证明"没有删掉任何功能"，只是换了位置：

| 原功能 | 原位置 | 新位置 |
|---|---|---|
| 文件夹 增/删/改、颜色图标、卡片数、删除确认 | 首页 | 文件夹管理页 |
| 文件夹拖动排序 | *（未实现，P1）* | 文件夹管理页 ✅ 新增 |
| 卡片新建 | 文件夹详情页 `＋` | 首页悬浮 ✎ |
| 卡片删除 + 确认 | 右滑 | 首页右滑 / 长按菜单 / 笔记页 `⋯` |
| 按 updatedAt 倒序 | — | 不变 |
| 置顶 / 取消置顶 | 左滑 + 编辑器 pin 按钮 | 首页左滑 + 长按菜单 + 笔记页 `⋯` |
| 置顶徽章 | 折叠条 pin 图标 | 首页行标题前 |
| **移动到其它文件夹** | *（未实现，P1）* | 首页长按菜单 + 笔记页导航栏 ▾ ✅ 新增 |
| 标签 增/删 | 编辑器常驻 Section | 笔记页底部 `#` 按钮召唤 |
| 标签筛选 | 文件夹详情页 chip 行 | 搜索页 chip 行 |
| AI 主题一键转标签 | 摘要贴纸内按钮 | 摘要展开态直接点主题 chip |
| 全局搜索 | 文件夹列表页 🔍 | 首页 🔍（覆盖面更广） |
| 块 增/删/排序 | `＋菜单` / swipe / EditButton | 底部工具条 / 长按菜单 / 拖拽 |
| Markdown 富文本编辑 + 预览 | 每块切换按钮 | 聚焦/失焦自动切换 |
| 录音、播放、拖动进度、波形 | — | 不变 |
| 转写 / 重新转写 / 编辑转写稿 | DisclosureGroup + 2 个按钮 | 块内直显 + 长按菜单 |
| 图片全屏、图片说明 | 常驻输入框 | 点击全屏 / 长按加说明 |
| 文档 QuickLook 预览 | — | 不变 |
| 链接应用内打开 / Safari / 复制 | 点击 + contextMenu | 不变（额外把编辑网址并入长按） |
| AI 初始总结生成 | 首次展开小三角 | 退出笔记页自动 + `⋯` 手动 |
| 增量更新总结 | 退出编辑器自动 | 不变 + `⋯`「立即更新」 |
| 重新生成完整总结 + 确认 | 贴纸内按钮 | `⋯` 菜单 |
| 清除 AI 摘要 + 确认 | 贴纸内按钮 | `⋯` 菜单 |
| 未读更新红点 | 小三角旁 | 首页行右侧 + 摘要条 |
| 摘要错误「重试」/「去设置」 | 贴纸内 | 摘要条内一行 |
| 首次 AI 隐私弹窗 | — | 不变 |
| 音频上传同意弹窗 | — | 不变 |
| 导出 Markdown / 纯文本 | 编辑器底部 share 菜单 | 笔记页 `⋯` + 首页长按菜单 |
| 设置全部项 | 一屏平铺 | 设置页 + 「高级」二级页 |
| 各处空状态引导 | — | 不变 |

**净变化：功能 +2（移动到文件夹、文件夹拖动排序），功能 −0。**

---

## 9. 关键路径对比

| 任务 | 现在 | 改后 |
|---|---|---|
| 写一条新笔记 | 首页 → 点文件夹 → `＋` → 点「添加内容」→ 选「文字」→ 打字 **(5 步)** | 点 `✎` → 打字 **(1 步)** |
| 加一张图 | 「添加内容」→ 「从相册选图」→ 选 **(3 步)** | 点 `📷` → 选 **(2 步)** |
| 看某条笔记的 AI 摘要 | 首页 → 文件夹 → 找到行 → 点小三角 **(4 步)** | 点行 → 点摘要条 **(2 步)** |
| 改一条笔记的归属文件夹 | **做不到** | 导航栏 `📁▾` → 选 **(2 步)** |
| 清除某条的 AI 摘要 | 首页 → 文件夹 → 小三角展开 → 滚到底 → 清除 → 确认 **(6 步)** | `⋯` → 清除 → 确认 **(3 步)** |
| 搜索（人在文件夹里时） | 返回首页 → 🔍 **(2 步)** | 🔍 **(1 步)** |

---

## 10. 落地顺序（每一阶段都可编译、可运行、可回退）

| 阶段 | 内容 | 影响文件 |
|---|---|---|
| **A. 首页重构** | 新建 `NoteListView` 作为根；`FolderListView` 改名/降级为 `FolderManageView`；新笔记行；文件夹 chip 行；未归类 = `folder == nil` | `RootView`, 新 `NoteListView`, `FolderListView`, `CardRowView` |
| **B. 笔记页合并** | 摘要条并入编辑器顶部；`⋯` 菜单收口；导航栏文件夹选择器；删除 EditButton | `CardEditorView`, `SummaryStickerView` |
| **C. 输入模型** | 底部工具条替代 `＋` 菜单；末尾恒有空文字块；插入后自动补块并聚焦 | `CardEditorView` |
| **D. 块内简化** | 文字块去切换按钮；链接块去常驻输入框；录音块转写直显；统一长按菜单 | `Blocks/*` |
| **E. 生成时机** | `ensureBaseIfNeeded` 从 `onAppear` 移到退出流程 | `SummaryStickerView`, `CardEditorView` |
| **F. 搜索与设置** | 搜索页标签 chip 行；设置页「高级」二级页 | `SearchView`, `SettingsView` |
| **G. 新增功能** | 移动到文件夹；文件夹拖动排序 | `NoteListView`, `FolderManageView` |
| **H. 验证** | `swift run mosaic-checks` + `xcodebuild build` + 模拟器截图走查 | — |

`CardListView.swift` 在阶段 A 结束后删除。

---

## 11. 已确认的决策

| # | 议题 | 结论 |
|---|---|---|
| 1 | 导航结构 | 全部笔记流为首页，文件夹降为筛选 chip |
| 2 | AI 摘要位置 | 移进笔记页顶部（可折叠），列表只留一句话 |
| 3 | 编辑器输入 | 备忘录式：默认在打字，媒体从底部工具条插入 |
| 4 | 视觉风格 | 系统原生极简，低频功能收进 `⋯` 菜单 |
| 5 | 新建归属 | 自动进「未归类」，笔记页内可改（用 `folder == nil` 实现） |
| 6 | 摘要生成时机 | 退出笔记页时自动生成（初始总结与更新记录合并为同一条规则） |
| 7 | 筛选栏 | 只留一排文件夹 chip，标签筛选移到搜索页 |
| 8 | 列表行密度 | 标题 + 一句话 + 时间（去掉类型图标与标签 chip） |

### 由上述决策推导、我自行拍板的次级细节（如有异议请指出）

- 文字块的「编辑/预览」改为**失焦渲染、聚焦显示源码**，而不是保留按钮或永远显示源码。
- 块的删除/排序统一为**长按菜单 + 拖拽**，删除 `EditButton` 模式。
- 链接块去掉常驻「编辑网址」输入框，改到长按菜单。
- 录音块转写稿**默认直接显示**（超 3 行折叠），不再默认藏在 DisclosureGroup 里。
- 主题关键词 chip 由**只读**改为**可点即加为标签**，删除独立按钮。
- 文件夹 chip **不显示卡片数**。
- 「未归类」用 `folder == nil` 表达，不创建实体文件夹行。
