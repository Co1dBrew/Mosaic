# Mosaic 检索评测方法 · EVAL_SPEC

> Source of Truth（`README.md` §Source of Truth 里的第五份）。
> 讲的是**怎么测**：数据集怎么来、指标怎么算、哪一部分可以调、哪一部分不能碰。
> 发布判定的阈值在 `GATE_POLICY.md`，不在这里。

---

## 0. 一句话

当前评测集是 **`scenario-v5`**：242 篇合成语料 + **207 条 agent 编写的场景化 query**，
覆盖九类检索能力，带分级相关性、硬负例与 development / holdout 划分。

**它不是真实用户数据。** 见 §1。

---

## 1. 数据来源标记（最重要的一节）

数据集里每一条用例都带：

```json
"provenance": "agent_authored_realistic"
```

含义：**query 与新增笔记由 agent 按真实使用场景编写**，语料是合成的。

可以这样称呼它：

```
场景化评测集 · production-style cases · human-like evaluation cases
scenario-authored evaluation
```

**不可以**这样称呼它：

```
real user logs · actual user queries · human collected
organic traffic · production traffic · 真实用户标注
```

它能证明的：评测管线闭合、覆盖结构成立、两套配置之间**可比较**。
它不能证明的：任何关于真实用户 Recall 的绝对结论。

数据文件自身携带这句声明（`disclaimer` 字段），且有断言守着 ——
文档会被人忘记，数据会被拷走。

---

## 2. 语料（242 篇）

| 类型 | 篇数 | 中 / 英 |
|---|---:|---|
| text | 49 | 28 / 21 |
| ocr | 48 | 23 / 25 |
| transcript | 45 | 24 / 21 |
| extracted | 49 | 28 / 21 |
| link | 51 | 25 / 26 |

- 六篇**长语料**（>600 字符），每篇带中段（40%–60%）与末段（>80%）锚点。
  chunk 策略实验靠它们 —— 只有第一块的用例证明不了「切分把话切断了」。
- **同构簇**：主题几乎一样、只差一个关键事实的笔记组。共 18 组，其中 **10 组 ≥6 篇**。

> ### 为什么簇一定要 ≥6 篇
>
> v4 的簇是 3 篇，而 Top-5 装得下整簇 —— 于是 in-scope R@5 恒为 **1.000**，
> 这个指标什么都没测到。v5 把主要的簇扩到 6–8 篇，Top-5 装不下整簇，
> R@5 才重新有区分度。
>
> 仍有 8 组是 3–5 篇。它们是真实存在的近似组，但**在那些簇上 R@5 没有区分度，
> 只有 R@1 有** —— 报告里不要用它们支持 R@5 的结论。

---

## 3. 查询（207 条）

### 3.1 九类能力

| 类别 | 条数 | 占比 | 考什么 |
|---|---:|---:|---|
| `semantic_recall` | 42 | 20.3% | 记得意思，不记得原词 |
| `exact_fact` | 30 | 14.5% | 日期 / 金额 / 编号 / 版本号 |
| `lexical_trap` | 30 | 14.5% | query 与**错误**笔记词面重合更多 |
| `near_duplicate` | 30 | 14.5% | 同构簇里只有一篇是答案 |
| `cross_language` | 25 | 12.1% | 中文 query 找英文笔记，或反过来 |
| `noisy_query` | 20 | 9.7% | 错别字 / 缩写 / 口语 / 实体记错一半 |
| `contextual_recall` | 10 | 4.8% | 只记得场景 |
| `negative` | 10 | 4.8% | 语料里没有答案 |
| `ambiguous` | 10 | 4.8% | 两个候选都成立 |

分类不是「难度的另一种说法」。`lexical_trap` 与 `near_duplicate` 都可以是 hard，
但**失败原因完全不同**：一个是词面对抗，一个是排序区分度。
分类的价值在于回答「这一轮 60% 的失败集中在哪一类」。

### 3.2 语言（检索方向）

| | 条数 | 占比 |
|---|---:|---:|
| 中文 → 中文 | 91 | 44.0% |
| 英文 → 英文 | 91 | 44.0% |
| 跨语言 | 25 | 12.1% |

跨语言是**已知架构边界**，它不进 Gate 主指标。占比受控在 8%–15%：
让它膨胀会把一个诊断分组悄悄变成主分母。

### 3.3 难度

easy 11.7% · medium 47.7% · hard 40.6%。
**每一类语料都有三档**（有断言守着）。

### 3.4 每条用例带什么

```json
{
  "id": "ND07",
  "query": "延期到底批了没有",
  "scenario": "等了两周还没消息，想确认最新状态",
  "category": "near_duplicate",
  "difficulty": "hard",
  "contentType": "link",
  "expectedNoteIDs": ["L38"],
  "relevance": { "L38": 3, "L39": 0, "L50": 0, "L51": 0, "L53": 0 },
  "hardNegativeNoteIDs": ["L39", "L50", "L51", "L53"],
  "expectedBehavior": "返回「已经批准」那封，不是补材料或审核中",
  "expectedLandingTarget": "block:L38-block",
  "rationale": "七封同主题邮件，标题一模一样，只有结论不同",
  "provenance": "agent_authored_realistic",
  "split": "holdout"
}
```

- **`scenario` 是必填的**：没有场景就没法让第二个人复核「这条像不像真人问的」。
- **`hardNegativeNoteIDs` 不是随机负例**：每一条都要能说出「为什么容易误判」
  （同一份合同的另一版 / 同一个人的另一次通话 / 同一格式的另一张单据）。
  medium / hard 的用例必须有（`cross_language` 除外，那一类的失败原因是语种覆盖，
  硬凑干扰项是在编造理由）。

---

## 4. 分级相关性

```
3 = 就是它        2 = 相关        1 = 部分相关        0 = 不相关但容易误判
```

现有指标体系是 binary 的，映射规则：**≥2 分算相关**。

1 分（部分相关）既不算命中也不算错误 —— 把「勉强沾边」算成命中会让 Recall 虚高，
算成错误又会惩罚一个合理的结果。它保留在数据里，将来上 nDCG 时不必重新标注。

**歧义用例刻意保留两个 ≥2 分的答案。** Recall 的口径是「全部 expected 都要在 Top K」，
所以歧义用例会要求系统把两个都召回。这是有意的严格：面对一条歧义 query，
只返回其中一个而漏掉另一个，对用户来说就是一次「没找到」。

---

## 5. Development / Holdout

| | 条数 | 占比 |
|---|---:|---:|
| development | 144 | 69.6% |
| holdout | 63 | 30.4% |

**划分方式是确定性的**：按类别分层，组内按 id 排序后每 10 条取第 1 / 5 / 8 条。
不用随机数 —— 随机划分每次重跑都换一批，「冻结」就无从谈起。

### 用法

| | development | holdout |
|---|---|---|
| debug / 归因分析 | ✅ | ❌ |
| 调参数（topK / fusion / chunk） | ✅ | ❌ |
| 发布判定 | ❌ | ✅ |

**禁止**针对某一条 holdout 用例加特殊逻辑。holdout 上失败时只能做
**可泛化的修复**，然后重跑；如果修复只对那一条有效，它是 evaluation overfit，必须撤销。

### 冻结指纹

数据集带 `checksum`（sha256），并且**同一个值写死在
`ScenarioDatasetChecks.frozenChecksum` 里**。任何改动都会让这条断言失败。

改数据的正确流程：

```bash
python3 tools/eval/build_scenario_set.py
```

把它打印的 checksum 抄进 `ScenarioDatasetChecks.frozenChecksum`，
并在提交信息里说明改了什么。指纹如果是现算的，任何改动都会自洽 —— 那它就只是个装饰。

---

## 6. 指标口径

| 指标 | 口径 |
|---|---|
| 命中 | 期望笔记出现在 Top K 的**笔记去重列表**里（一篇笔记的 3 个 chunk 都进 Top 5 只算一次，否则长笔记会凭 chunk 多而虚高） |
| Recall@K | 命中的正例数 / 正例总数。多个 expected 时**全部**都要在 Top K |
| MRR | 第一个正确笔记的名次倒数；一条都没命中记 0 |
| No-result accuracy | 负例中返回空列表的比例。当前严格以 `results.isEmpty` 判定 |
| P50 / P95 | **只统计检索本身**，不含评测框架、不含 UI 动画 |

分组：

- **in-scope**：Gate 的主分母
- **cross-language**：已知架构边界，只作诊断
- **negative**：不进 Recall / MRR 的分母

---

## 7. 数据质量自检

生成时（Python）与每次跑 checks 时（Swift）**各查一遍**。
生成脚本查的是「产出对不对」，Swift 断言查的是「落盘之后有没有被手改」——
手改不会重跑脚本。

自动检查覆盖：

```
重复 query · 重复笔记正文 · 模板化（字符三元组相似度 ≥0.80）
标注一致性（分级 ↔ expected ↔ contentType ↔ 落点前缀）
引用有效性（expected / 硬负例都指向存在的笔记）
类别 / 语言 / 难度 / split 分布
每类语料三档难度齐全 · 每个大簇都有用例打进去
泄漏（query 被逐字写进笔记）
```

### 关于泄漏检查的一个例外

`exact_fact` 这一类的定义就是「用户记得原词」——「CF259X High Yield 10000 pages」
「VNB3K21099」本来就是笔记上印着的字。把它判成泄漏等于取消这一整个类别。
例外**只对 `exact_fact` 开**，其余类别一旦逐字出现即为数据错误。

### 一个被修正的检查

第一版的模板化检查用正则把实体替换成 `X` 再比对骨架。它在中文上完全失效
（中文没有空格，正则一归一化就把整句压成一个 `X`），把 67 条完全不同的 query
报成同一个模板。改成**字符三元组 Jaccard 两两比对**：语言无关，
而且直接对应要防的东西 —— 抄一条 query 只换实体，相似度必然很高。

---

## 8. 性能语料（与本文件的评测集分开）

`IndexingCostAtScale` 用生成式扩展的语料测 1k / 5k / 10k / 20k 四档的
建索引耗时与常驻内存。**它与相关性无关**，不要把两者的数字混在一张表里：
一个回答「够不够快」，一个回答「找不找得到」。

---

## 9. 已知限制

1. **语料是合成的。** 真实笔记的噪声（排版混乱、半截句子、复制粘贴的网页残留）
   没有被覆盖。
2. **标注是单人的。** §7 的自动检查能查一致性，查不出系统性偏见。
   正式发布前需要第二个人**盲审**部分 expected（不看检索排名）。
3. **8 个簇仍是 3–5 篇**，那些簇上的 R@5 没有区分度。
4. **没有真实用户的 query 分布。** 九类的配比是设计判断，不是观测结果。

以上四条都不能靠加数据解决，需要真实用户数据。在那之前，
本评测集的结论只用于**两套配置之间的比较**，不用于绝对水平的承诺。
