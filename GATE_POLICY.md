# Mosaic 发布判定 · GATE_POLICY v1

> Source of Truth。**发布标准写在这里，只写在这里。**
> 评测方法在 `EVAL_SPEC.md`；这份文件只回答「达到什么条件才允许上线」。
>
> 判定实现：`Sources/MosaicKit/Eval/ReleaseGate.swift`，
> 阈值常量 `GateThresholds.v1`。

---

## 0. 判定规则

**任一项 FAIL 即阻断。不做加权，不算总分。**

加权总分会制造「大部分指标都很好」的错觉，而 Release Gate 存在的唯一意义就是
拦住这种错觉。判定就是 `checks.allSatisfy(\.passed)`，那一行代码是整个模块的产品主张。

---

## 1. 四行检查

| 行 | 判什么 | 阻断 |
|---|---|---|
| **Recall @1/@5** | 相对 baseline **无统计显著退化** | ✅ |
| **MRR** | 相对 baseline **无统计显著退化** | ✅ |
| **Absolute Floor** | R@1 / R@5 / MRR 的**绝对下限** | ✅ |
| **Metric A · First Result** | 延迟分层预算（只在合格测量环境下判） | ✅ |
| **Regression** | 回归集通过率 ≥ 98% | ✅ |

`DECISION_CONFIG` 要求的三件套各自对应哪一行：

| 要求 | 对应 | 拦住什么 | 拦不住什么 |
|---|---|---|---|
| `BASELINE_RELATIVE` | Recall / MRR | 新配置把老配置弄坏了 | baseline 自己很差 |
| `HARD_MINIMUM_FLOOR` | Absolute Floor | 两个配置都很差 | 小幅但真实的退化 |
| `STATISTICAL_CONFIDENCE` | Recall / MRR 的 CI | 把抖动当成退化 | 样本量不够时什么都判不出 |

三项互补，缺一个就有一类问题穿过去。

---

## 2. 统计置信：为什么需要，怎么算

### 为什么

Gate v0 只比较两个点估计，零容差。在 in-scope 67 条用例上，
**一条用例翻面 = 0.0149** —— 任何一条边缘用例的排名波动都会阻断发布，而那不是质量信号。

反过来，给点估计留一个固定容差（比如 0.010）同样是错的：
容差该有多大取决于**样本量与用例之间的方差**，不是一个可以拍出来的常数。

### 怎么算

**配对 bootstrap**（`PairedBootstrap`）：

1. 两次跑批用同一组用例，逐条按 `caseID` 配对，算每条的指标差
2. 对差值序列有放回重采样 2000 次，取每次的均值
3. 均值分布的 2.5% / 97.5% 分位数即 95% 置信区间

**只有区间整体落在 0 以下才算真的退化。** 区间跨过 0 时数据分不清退化与抖动。

### 三个实现约束

- **配对**：同一组用例，消掉用例难度差异。不配对的话「这批用例本来就难」会被算进方差，区间会宽到什么都判不出来。
- **bootstrap 而不是 t 检验**：Recall 是 0/1，MRR 取值在 `{0, 1, 1/2, 1/3, …}`，都不接近正态，而样本量只有几十到两百。
- **固定种子**：用 SplitMix 而不是系统随机源。一个每次重跑都给不同答案的发布判定，没有人会相信它。

### 逐用例结果缺席时

退回点估计比较，并在 `detail` 里说明。**不能因为算不了置信区间就放行** ——
那会把一个数据缺口变成一次通过。

---

## 3. 绝对下限：取值与依据

```
R@1 ≥ 0.35    R@5 ≥ 0.40    MRR ≥ 0.38
```

### 依据

这三个数字是**产品判断**，不是从 baseline 推出来的（从 baseline 推出来的东西
属于上一节，那是相对比较）：

- **R@1 ≥ 0.35** —— 三次搜索里至少有一次「第一条就是对的」。低于这个数，
  用户对「智能搜索」的预期会持续落空，产品叙事撑不住。
- **R@5 ≥ 0.40** —— Top-5 正好是一屏。这一条说的是「至少能在一屏之内找到」。
- **MRR ≥ 0.38** —— 等价于正确答案平均排在第 2.6 位。

### 必须一起说清楚的事

写下这三个数字的时点，候选（local-hybrid）在 holdout 上实测
**R@1 0.365 · R@5 0.423 · MRR 0.412**。

也就是说**当前候选只是刚过线**，余量分别是 `+0.015 / +0.023 / +0.032`。
holdout 有 52 条 in-scope 正例，**一条用例翻面 = 0.019** —— 一条边缘用例掉下去
就能把 R@1 打到线下。

**这不是「通过」，这是「勉强及格」。** 在 PRD 给出定值之前，
不要拿这条 PASS 去支持任何质量承诺。

### 修改规则

改这三个数字要：版本号 bump（`gate-v1` → `gate-v2`）· 在这里写新依据 ·
**重跑 baseline**。

**不允许因为候选没过就下调它们。** 候选没过时的流程是：
查根因 → 做可泛化的修复 → 重跑。

---

## 4. 延迟

沿用分层性能预算 `perf-v2`（`PerformanceGatePolicy`）：

| 层 | P50 | P95 | 阻断 |
|---|---:|---:|---|
| Metric A · 第一批可交互结果 | 100 ms | 250 ms | ✅ |
| Metric B-local · 本地语义 | — | 250 ms | ✅ |
| Metric B-cloud · 含网络 | — | 记录不判定 | ❌ |

云端层不设阈值：那一层的耗时由地理位置决定（实测网络占 **99.87%**），
而 progressive enhancement 下已有 keyword 兜底。

### 测量环境闸门

`perf-v2` 要求 **release 构建 + 真机**。不满足时判 **STALE 而不是 FAIL** ——
「这批数字没有资格参与判定」与「性能不达标」是两件事，补救动作也不同
（一个是换台机器重测，一个是去优化代码）。

### `perf-none`：质量判定不受环境闸门约束

比较两套配置的**检索质量**时用 `PerformanceGatePolicy.qualityOnly`，
它不检查环境，也**不出延迟那一行**。

理由：环境闸门是给延迟结论设的，而质量结论不受构建配置影响 ——
v4 那一轮真机与 Mac 的质量数字**逐位相同**，差别只在延迟。
用延迟的环境要求去卡质量比较，结果是每次质量跑批都判 STALE，
于是没有人再看它 —— 而「一个总是被跳过的 Gate 等于没有 Gate」。

**`perf-none` 不能用来声称延迟达标。** 延迟只在真机 release 的 `MosaicBench` 上有结论。

---

## 5. 本轮判定结果（`scenario-v5` · Mac · 本机 NLEmbedding）

### 配置

| | |
|---|---|
| baseline | `keyword`（当前生产的词法快通道） |
| candidate | `local-hybrid`（keyword + 本机句向量，weighted 0.7 / 0.3） |
| cloud arm | **NOT RUN — MISSING CREDENTIALS** |

baseline 选 keyword 而不是随机排序：**基线必须是候选真正准备替代的那一套**。
选一个容易赢的软基线会让任何改动都显得成功。

### 数字

| arm | split | n | R@1 | R@3 | R@5 | MRR | 负例克制率 |
|---|---|---:|---:|---:|---:|---:|---:|
| keyword | development | 120 | 0.292 | 0.308 | 0.325 | 0.310 | 85.7% |
| keyword | holdout | 52 | 0.346 | 0.365 | 0.365 | 0.365 | 100% |
| local-hybrid | development | 120 | 0.292 | 0.333 | 0.358 | 0.330 | 0% |
| local-hybrid | holdout | 52 | **0.365** | 0.404 | **0.423** | **0.412** | 0% |

### 判定

| split | 结论 | 原因 |
|---|---|---|
| **development** | **PROMOTION BLOCKED** | R@1 0.292 < 0.350（绝对下限） |
| **holdout** | PASS | 四项全绿，但见下 |

**整体结论：BLOCKED。**

两个 split 判定不一致本身就是结论的一部分：
**holdout 比 development 容易**（R@1 0.365 vs 0.292，差 0.073）。
一个在 development 上过不了线的配置，不该因为 holdout 恰好宽松就放行。

### 统计置信的读数

候选相对 baseline **没有统计显著退化**，而且在两个 split 上都有显著**提升**：

```
development   R@5  Δ +0.033  [+0.000, +0.075]      MRR  Δ +0.020  [+0.007, +0.035]
holdout       R@5  Δ +0.058  [+0.000, +0.135]      MRR  Δ +0.046  [+0.012, +0.098]
```

R@1 的 delta 是 `+0.000 [0, 0]`（development）—— 本机语义路对**第一名**没有任何贡献。
这与 v4 的结论一致：本地句向量在中文上很弱，真正的质量来自云端臂。

### 按类别切片（development · R@1）

| 类别 | baseline | candidate |
|---|---:|---:|
| exact_fact | 0.952 | 0.952 |
| semantic_recall | 0.379 | 0.379 |
| contextual_recall | 0.143 | 0.143 |
| near_duplicate | 0.095 | 0.095 |
| lexical_trap | 0.048 | 0.048 |
| noisy_query | 0.000 | 0.000 |
| ambiguous | 0.000 | 0.000 |

**这张表是 v5 相对 v4 最大的实用差别。**「总体 0.292」说明不了该改什么；
「lexical_trap 0.048 而 exact_fact 0.952」说明得很清楚：
系统能找到原词，找不到换了说法的同一件事。

---

## 6. 本轮修掉的一个缺陷（评测集发现的）

### 现象

`scenario-v5` 首次跑批：`exact_fact` R@1 **0.905**，而 `near_duplicate` /
`noisy_query` / `contextual_recall` **整类 0.000**。抽样看实际返回是纯噪声。

### 根因

`SearchMatcher.tokens(from:)` **只按空白切分**。一句没有空格的中文因此是
**一个 token**，而匹配要求每个 token 作为子串出现（AND）——
于是「整句原样出现在某篇笔记里」成了命中的必要条件。

v4 的评测集看不出来，因为它的中文 query 大多是「延期毕业 书面申请」这种
**带空格的关键词组合**。v5 把「真人会怎么打字」写进去之后，它立刻暴露了。

### 修复

CJK 连续段切成**重叠二元组**，按 `minimum_should_match` 判定
（`QuerySegmentation` / `CJKMatchPolicy`）。不建任何索引、不改存储、不改 chunk。

> 与 `RETRIEVAL_ARCHITECTURE` §26.6「不做 n-gram 索引」不冲突：
> 那条说的是**索引侧**（建索引成本、匹配单位），这里改的是**查询侧切分**。

### 参数怎么定的

在 **development** 上扫描（`QuerySegmentationSweep`，`MOSAIC_SEG_SWEEP=1` 打开）：

| policy | R@1 | 负例克制率 |
|---|---:|---:|
| whitespace（旧） | 0.175 | 100% |
| r=0.34 f=1 | 0.250 | 100% |
| **r=0.15 f=3（选中）** | **0.292** | **90%** |
| r=0.25 f=1 | 0.317 | 80% |
| r=0.15 f=1 | 0.358 | 70% |

R@1 与负例克制率是**互相交换**的。选择依据是一条明写的产品约束：

> **负例克制率相对当前生产（100%）下降不得超过 10pp。**

理由是生产里没有相关性下限做兜底 —— abstention 目前是
`EXPERIMENT_ONLY_NOT_PRODUCTION`（TD-10 未解决），
「搜什么都有结果」没有第二道防线。

在这条约束下取 R@1 最高的一格。更松的两格（0.317 / 0.358）留着，
**等 abstention 能上生产再回来取**。

### 一条被修正的旧断言

`RetrievalProviderBenchmark` 原来要求 keyword 对无答案 query 的克制率
**恰好 100%**，并称它是 no-result 的唯一保障。

100% 这个水平其实是分词缺陷的**副产品**：一个从不匹配中文自然句的词法路，
对中文无答案 query 当然永远返回空。它测到的不是「系统会克制」，
而是「系统查不动中文」。

阈值改为 **≥ 0.85**，保留「keyword 路显著克制」这个可证伪的主张。
真正解决误报要靠相关性下限，不是靠一个查不动中文的分词器。

---

## 7. 还缺什么才能真的上线

| # | 缺口 | 谁能做 |
|---|---|---|
| 1 | **development 上 R@1 未达绝对下限**（0.292 < 0.350） | 工程：云端臂是已知的质量来源，需要凭据复跑 |
| 2 | **云端臂 NOT RUN** | 用户：提供 embedding 凭据 |
| 3 | **延迟未在真机 release 上复测** | 用户：连真机跑 `MosaicBench` |
| 4 | **评测集仍是 agent 编写的场景化用例** | 用户：真实笔记 + 人工标注（见 `EVAL_SPEC.md` §9） |
| 5 | **回归集为空**，Regression 那一行恒过 | 工程：从失败用例里挑进回归集 |

第 4 条最重要：**Gate PASS 只说明判定链路闭合，不说明真实用户体验达标。**
