# Mosaic 发布判定 · GATE_POLICY v1

> **v1 已关闭**（`PROJECT_CLOSURE.md`）。这份文件的内容随之冻结 ——
> 改动 Gate 需要先满足 `HANDOFF_NEXT.md` 里的重新进入条件。
>
> Source of Truth。**发布标准写在这里，只写在这里。**
> 评测方法在 `EVAL_SPEC.md`；这份文件只回答「达到什么条件才允许上线」。
>
> 判定实现：`Sources/MosaicKit/Eval/ReleaseGate.swift`，
> 阈值常量 `GateThresholds.v1`。

---

## 0. 判定规则

**任一项 FAIL 即阻断。不做加权，不算总分。**

加权总分会制造「大部分指标都很好」的错觉，而 Release Gate 存在的唯一意义就是
拦住这种错觉。判定就是 `checks.allSatisfy(\.passed)`，那一行代码是整个模块的产品主张。

---

## 1. 四行检查

| 行 | 判什么 | 阻断 |
|---|---|---|
| **Recall @1/@5** | 相对 baseline **无统计显著退化** | ✅ |
| **MRR** | 相对 baseline **无统计显著退化** | ✅ |
| **Absolute Floor** | R@1 / R@5 / MRR 的**绝对下限** | ✅ |
| **Metric A · First Result** | 延迟分层预算（只在合格测量环境下判） | ✅ |
| **Regression** | 回归集通过率 ≥ 98% | ✅ |

`DECISION_CONFIG` 要求的三件套各自对应哪一行：

| 要求 | 对应 | 拦住什么 | 拦不住什么 |
|---|---|---|---|
| `BASELINE_RELATIVE` | Recall / MRR | 新配置把老配置弄坏了 | baseline 自己很差 |
| `HARD_MINIMUM_FLOOR` | Absolute Floor | 两个配置都很差 | 小幅但真实的退化 |
| `STATISTICAL_CONFIDENCE` | Recall / MRR 的 CI | 把抖动当成退化 | 样本量不够时什么都判不出 |

三项互补，缺一个就有一类问题穿过去。

---

## 2. 统计置信：为什么需要，怎么算

### 为什么

Gate v0 只比较两个点估计，零容差。在 in-scope 67 条用例上，
**一条用例翻面 = 0.0149** —— 任何一条边缘用例的排名波动都会阻断发布，而那不是质量信号。

反过来，给点估计留一个固定容差（比如 0.010）同样是错的：
容差该有多大取决于**样本量与用例之间的方差**，不是一个可以拍出来的常数。

### 怎么算

**配对 bootstrap**（`PairedBootstrap`）：

1. 两次跑批用同一组用例，逐条按 `caseID` 配对，算每条的指标差
2. 对差值序列有放回重采样 2000 次，取每次的均值
3. 均值分布的 2.5% / 97.5% 分位数即 95% 置信区间

**只有区间整体落在 0 以下才算真的退化。** 区间跨过 0 时数据分不清退化与抖动。

### 三个实现约束

- **配对**：同一组用例，消掉用例难度差异。不配对的话「这批用例本来就难」会被算进方差，区间会宽到什么都判不出来。
- **bootstrap 而不是 t 检验**：Recall 是 0/1，MRR 取值在 `{0, 1, 1/2, 1/3, …}`，都不接近正态，而样本量只有几十到两百。
- **固定种子**：用 SplitMix 而不是系统随机源。一个每次重跑都给不同答案的发布判定，没有人会相信它。

### 逐用例结果缺席时

退回点估计比较，并在 `detail` 里说明。**不能因为算不了置信区间就放行** ——
那会把一个数据缺口变成一次通过。

---

## 3. 绝对下限：取值与依据

```
R@1 ≥ 0.35    R@5 ≥ 0.40    MRR ≥ 0.38
```

### ⚠️ 这三个数字是 **PROVISIONAL HARD FLOORS**

> These thresholds are engineering/product **provisional** thresholds based on the
> current **scenario-authored** evaluation corpus. They **must be recalibrated**
> after human-authored evaluation becomes available.
>
> 中文同义：**这三个下限是临时值。** 它们的依据是当前这份
> **agent 编写的场景化评测集**（`provenance = agent_authored_realistic`），
> 不是真实用户数据。拿到人工编写的评测集之后**必须重新校准**。
>
> 「临时」不等于「可以随便改」——
> 改动规则见本节末尾，任何一次改动都要 bump 版本号并重跑 baseline。

`GATE_POLICY_VERSION = V1_PROVISIONAL`

| 项 | 值 | 角色 |
|---|---:|---|
| Recall@1 | ≥ 0.35 | **主指标** |
| MRR | ≥ 0.38 | **主指标** |
| Recall@5 | ≥ 0.40 | 安全网（v2 起不再是主指标，见 §5） |
| P95 | < 250 ms | 性能天花板（用户可感知的搜索延迟） |

### 依据

这三个数字是**产品判断**，不是从 baseline 推出来的（从 baseline 推出来的东西
属于上一节，那是相对比较）：

- **R@1 ≥ 0.35** —— 三次搜索里至少有一次「第一条就是对的」。低于这个数，
  用户对「智能搜索」的预期会持续落空，产品叙事撑不住。
- **R@5 ≥ 0.40** —— Top-5 正好是一屏。这一条说的是「至少能在一屏之内找到」。
- **MRR ≥ 0.38** —— 等价于正确答案平均排在第 2.6 位。

### 必须一起说清楚的事

**这三个数字是在候选还没过线的时候定下来的。** 当时（CJK 切分修复之前）
候选在 holdout 上是 R@1 0.365 / R@5 0.423 / MRR 0.412，development 上是
0.292 / 0.358 / 0.330 —— **development 判 BLOCKED**。

之后修掉了两个词法路的缺陷（§6），候选升到 development 0.478 / 0.567 / 0.517、
holdout 0.604 / 0.673 / 0.627，才通过。

**顺序很重要**：阈值先定，然后去修代码，不是反过来。
可核对的证据是 commit `c406a00` 的说明 —— 它引入这三个阈值，
而它自己记录的判定结果是 **BLOCKED**（development R@1 0.292 < 0.350）。
阈值不是照着一个已经通过的结果反推出来的。

余量现在是舒服的（development 上 R@1 +0.128，about 15 条用例翻面才会掉到线下），
但这**不是**质量结论：评测集仍是 agent 编写的场景化用例（`EVAL_SPEC.md` §1）。

### 修改规则

改这三个数字要：版本号 bump（`gate-v1` → `gate-v2`）· 在这里写新依据 ·
**重跑 baseline**。

**不允许因为候选没过就下调它们。** 候选没过时的流程是：
查根因 → 做可泛化的修复 → 重跑。

---

## 4. 延迟

沿用分层性能预算 `perf-v2`（`PerformanceGatePolicy`）：

| 层 | P50 | P95 | 阻断 |
|---|---:|---:|---|
| Metric A · 第一批可交互结果 | 100 ms | 250 ms | ✅ |
| Metric B-local · 本地语义 | — | 250 ms | ✅ |
| Metric B-cloud · 含网络 | — | 记录不判定 | ❌ |

云端层不设阈值：那一层的耗时由地理位置决定（实测网络占 **99.87%**），
而 progressive enhancement 下已有 keyword 兜底。

### 测量环境闸门

`perf-v2` 要求 **release 构建 + 真机**。不满足时判 **STALE 而不是 FAIL** ——
「这批数字没有资格参与判定」与「性能不达标」是两件事，补救动作也不同
（一个是换台机器重测，一个是去优化代码）。

### `perf-none`：质量判定不受环境闸门约束

比较两套配置的**检索质量**时用 `PerformanceGatePolicy.qualityOnly`，
它不检查环境，也**不出延迟那一行**。

理由：环境闸门是给延迟结论设的，而质量结论不受构建配置影响 ——
v4 那一轮真机与 Mac 的质量数字**逐位相同**，差别只在延迟。
用延迟的环境要求去卡质量比较，结果是每次质量跑批都判 STALE，
于是没有人再看它 —— 而「一个总是被跳过的 Gate 等于没有 Gate」。

**`perf-none` 不能用来声称延迟达标。** 延迟只在真机 release 的 `MosaicBench` 上有结论。

---

## 5. 本轮判定结果（`scenario-v5` · Mac · 本机 NLEmbedding）

### 配置

| | |
|---|---|
| baseline | `keyword`（当前生产的词法快通道） |
| candidate | `local-hybrid`（keyword + 本机句向量，weighted 0.7 / 0.3） |
| cloud arm | **NOT RUN — MISSING CREDENTIALS** |

baseline 选 keyword 而不是随机排序：**基线必须是候选真正准备替代的那一套**。
选一个容易赢的软基线会让任何改动都显得成功。

### 数字

| arm | split | n | R@1 | R@3 | R@5 | MRR | 负例克制率 | P50 | P95 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| keyword | development | 120 | 0.478 | 0.525 | 0.558 | 0.513 | 85.7% | 13.6 ms | 26.0 ms |
| keyword | holdout | 52 | 0.604 | 0.654 | 0.673 | 0.625 | 100% | 15.7 ms | 30.3 ms |
| local-hybrid | development | 120 | 0.478 | 0.525 | 0.567 | 0.517 | 0% | 50.2 ms | 70.6 ms |
| local-hybrid | holdout | 52 | 0.604 | 0.635 | 0.673 | 0.627 | 0% | 50.1 ms | 68.2 ms |

### 判定

| split | 结论 |
|---|---|
| development | **PASS**（四项全绿） |
| holdout | **PASS**（四项全绿） |

### 但是：**PASS 的不是候选，是 baseline**

```
development   R@1  Δ +0.000  [+0.000, +0.000]  n=113
holdout       R@1  Δ +0.000  [+0.000, +0.000]  n=48
```

**本机语义路对第一名的贡献是精确的 0。** R@5 只有 +0.008，MRR +0.004，
两者的置信区间都跨过 0 —— 统计上无法与「没有区别」区分开。

而它的代价是确定的：**P50 从 13.6 ms 涨到 50.2 ms（3.7×）**，
中文库还要付云端上传的隐私成本。

> **产品结论：按现在的数据，`local-hybrid` 不值得发。**
> 该发的是 keyword。语义路要成立，得靠云端臂 —— 而云端臂 **NOT RUN**。
>
> Gate 判 PASS 是对的：它问的是「候选有没有把 baseline 弄坏」，答案是没有。
> **「值不值得」不是 Gate 的问题**，那是 Compare 的事（`DEVTOOLS.md` §4.6：
> Eval 只讲 trade-off，不下 PASS / FAIL）。两件事分开，所以这一段写在这里。

### 按类别切片（development · R@1）

| 类别 | baseline | candidate |
|---|---:|---:|
| exact_fact | 0.905 | 0.905 |
| semantic_recall | 0.621 | 0.621 |
| contextual_recall | 0.429 | 0.429 |
| noisy_query | 0.357 | 0.357 |
| near_duplicate | 0.333 | 0.333 |
| **lexical_trap** | **0.095** | **0.095** |

**这张表是 v5 相对 v4 最大的实用差别。**「总体 0.478」说明不了该改什么；
「lexical_trap 0.095 而 exact_fact 0.905」说明得很清楚：
系统能找到原词，找不到换了说法的同一件事 —— 而那正是语义路本该解决的问题，
也正是本机语义路没能解决的问题。

---

## 5b. Gate 与 Promotion 是两个问题

这一节把 §5 末尾那段话固定成规则，因为它每一轮都会被重新问一遍。

| | Gate | Promotion（产品决策） |
|---|---|---|
| 问的是 | 候选**有没有把系统弄坏** | 候选**值不值得替换生产** |
| 输入 | 绝对下限 · 相对 baseline · 统计置信 · 延迟天花板 · 回归集 | Gate 结论 **+ 收益/代价的量级** |
| 输出 | PASS / BLOCKED / STALE | PROMOTE / DO NOT PROMOTE |
| 谁定 | 代码（`ReleaseGate.evaluate`） | 人 |

**`PASS` + `DO NOT PROMOTE` 是一个合法且常见的状态。**
它说的是：「这个候选没坏，但它带来的好处不值它的代价。」

本轮就是这个状态：

```
local-hybrid   Gate       = PASS（Mac 与真机各判一次，都 PASS）
local-hybrid   Promotion  = NO
```

理由是量级：**质量增益 ≈ 0，延迟代价确定。** 详见 §5 与 §5c。

> **不要为了得到想要的产品结论去改 Gate。**
> 如果结论是「不该上候选」，正确的表达方式是 `PASS` + `DO NOT PROMOTE`，
> 而不是把阈值调到让候选 FAIL。后者会污染一个本来还能用来判断
> 「有没有弄坏」的工具 —— 而那正是 Gate 唯一的用途。

---

## 5c. 真机判定（iPhone Air · iOS 27.0 · Release）

Mac 上的数字**不能用来下延迟结论**（`perf-v2` 的环境闸门）。这一节是真机上跑出来的。

设备：`iPhone Air (iPhone18,4)` · iOS `27.0.0` · arm64 · Release ·
thermal `nominal` · 低电量模式关闭。

### 质量与延迟（同一次跑批 · scenario-v5 **holdout** · 242 notes / 278 chunks）

| arm | R@1 | R@3 | R@5 | MRR | P50 | P95 | 负例克制率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **keyword**（生产） | **0.604** | **0.654** | **0.673** | 0.625 | **2.29–2.36 ms** | **8.67–13.14 ms** | **100.0%** |
| local-hybrid（实验） | 0.604 | 0.635 | 0.673 | 0.627 | 8.96–9.48 ms | 16.03–16.75 ms | 0.0% |

质量数字两次跑批**逐位相同**（确定性评测集 + 确定性排序）；延迟给区间。

```
Gate（真机 · candidate local-hybrid · baseline keyword · layer semanticLocal）→ PASS
  ✅ Recall @1/@5   R@1 Δ +0.000 [+0.000, +0.000] n=48 · R@5 Δ +0.000 [+0.000, +0.000] n=52
  ✅ MRR            Δ +0.002 [-0.004, +0.010] n=52
  ✅ Absolute Floor R@1 0.604 · R@5 0.673 · MRR 0.627
  ✅ Metric B · Semantic  P50 9 ms · P95 17 ms（≤ 250 ms）
  ✅ Regression     100% · 3/3
```

### Promotion 决策：**NO**

三条理由，每一条都来自上面那张表：

1. **质量增益 ≈ 0。** R@1 与 R@5 的 delta 是精确的 `+0.000`（CI `[0, 0]`），
   MRR `+0.002` 的区间跨过 0。而 **R@3 反而更低**（0.654 → 0.635）。
2. **延迟代价确定：P50 约 +300%**（2.3 → 9.2 ms，两次跑批一致），P95 +27–85%。
   两者都远在 250 ms 天花板之内，但**天花板之内不等于免费** ——
   产品判断看的是相对量，不是有没有触顶。
3. **负例克制率从 100% 塌到 0%。** 这一条 Gate 不看，但它是三条里最严重的：
   keyword 对「本来就没有答案」的 query 会如实返回空，
   而 local-hybrid 把整个语料按余弦排下来，**每一条无答案的 query 都会拿到一屏结果**
   （TD-10：向量路没有相关性下限）。

> 第 3 条单独就足以否掉这次 promotion。它也解释了为什么 `abstention`
> 必须先能上生产，语义路才谈得上替换 keyword。

### 用户可感知延迟（Metric A · keyword 生产路径 · 真机 Release）

**两次独立跑批的区间**，不是单次读数 —— 单个数字会把这里的精度说得太满。

| chunks | 冷 P50 | 冷 P95 | 热 P50 | 热 P95 |
|---:|---:|---:|---:|---:|
| 1 000 | 4.60 – 4.72 ms | 11.18 – 12.01 ms | 2.96 – 3.22 ms | 4.36 – 4.67 ms |
| 5 000 | 19.59 – 19.67 ms | 27.93 – 29.39 ms | 15.56 – 15.80 ms | 22.68 – 23.69 ms |
| 10 000 | 38.63 – 38.95 ms | 58.50 – 58.71 ms | 33.69 – 34.19 ms | 48.48 – 51.02 ms |
| 20 000 | **81.54 – 90.99 ms** | **118.95 – 124.13 ms** | 67.12 – 74.79 ms | 102.20 – 112.78 ms |

预算：冷热两侧都要 P50 < 100 ms 且 P95 < 250 ms。**全部通过。**

**但余量要说准：** 最大档的冷 P50 是 81.5–91.0 ms，预算 100 ms ——
**余量只有 10–20%，而两次跑批之间就摆动了 9.5 ms。**
P95 那一侧余量 2.0×，是舒服的；P50 这一侧不是。

> **20k 是 PRD 的上限档。** 1k（真实个人笔记库更接近的规模）上冷 P50 只有 4.6 ms。
> 所以这不是「用户会遇到的延迟」，是「上限档上还剩多少空间」——
> 而剩下的空间不多了，下一次动词法路时要重测这一格。

> **这一轮先 FAIL 过一次。** 第一次真机跑批 20k 冷 P50 = **103.01 ms**，
> 超了 100 ms 的预算。根因不是「keyword 慢」，是**归一化每次查询重算全库**
> （占冷路径 35.4 ms），而它天然可并行却在串行跑。
> 并行折叠之后 103.01 → 81.54 ms。
>
> 同一轮还发现一个更要紧的东西：生产搜索**每次按键都新建一个 `RetrievalService`**，
> 于是那个归一化缓存**从来没有到达过用户** —— 线上永远走冷路径。
> 缓存实现了、单测守着、基准测过，唯独没接上。
> 两条路的结果逐位相同，所以没有任何既有测试会因此变红
> （`ProductionSearchTests.testNormalizedCacheSurvivesAcrossQueriesInASearchSession` 现在守着它）。

---

## 6. 本轮修掉的两个缺陷（都是评测集发现的）

### 6.1 中文 query 在词法路全灭

**现象**：`scenario-v5` 首跑，`exact_fact` R@1 0.905，而 `near_duplicate` /
`noisy_query` / `contextual_recall` **整类 0.000**，抽样看实际返回是纯噪声。

**根因**：`SearchMatcher.tokens(from:)` 只按空白切分。一句没有空格的中文因此是
**一个 token**，而匹配要求每个 token 作为子串出现（AND）——
于是「整句原样出现在某篇笔记里」成了命中的必要条件。

v4 看不出来，因为它的中文 query 大多是「延期毕业 书面申请」这种**带空格的
关键词组合**。v5 把「真人会怎么打字」写进去之后，它立刻暴露了。

**修复**：CJK 连续段切成**重叠二元组**，按 `minimum_should_match` 判定。

### 6.2 拉丁词的 AND 语义

**现象**：`noisy_query` 在 6.1 修好之后仍然是 0.000。

**根因**：拉丁 token 之间是 AND。`wheres the midterm now, snell or richards` ——
`wheres` 不在任何笔记里，于是**整条 query 不命中**。真人打字少一个撇号、
多两个词是常态。

**修复**：三个以上拉丁词时改为 `minimum_should_match`（命中一半即可）。
**两个词以内仍然是 AND** —— 否则「精确查两个词」也会变成模糊搜索。

### 6.3 参数怎么定的

全部在 **development** 上扫描（`QuerySegmentationSweep`，`MOSAIC_SEG_SWEEP=1` 打开）：

| policy | R@1 | 负例克制率 |
|---|---:|---:|
| whitespace（旧） | 0.186 | 100% |
| r=0.34 f=1 | 0.265 | 100% |
| r=0.15 f=3 | 0.310 | 90% |
| r=0.15 f=3 +latin | 0.354 | 90% |
| r=0.15 f=3 +latin L=0.67 | 0.389 | 90% |
| **r=0.15 f=3 +latin L=0.50（选中）** | **0.478** | **90%** |
| r=0.25 f=1 | 0.336 | 80% |
| r=0.15 f=2 +latin | 0.398 | 70% |

R@1 与负例克制率**互相交换**。选择依据是一条明写的产品约束：

> **负例克制率相对当前生产（100%）下降不得超过 10pp。**

理由是生产里没有相关性下限做兜底 —— abstention 目前是
`EXPERIMENT_ONLY_NOT_PRODUCTION`（TD-10 未解决），「搜什么都有结果」没有第二道防线。

在这条约束下取 R@1 最高的一格。三档放宽**都是同代价更优**：负例克制率一路都是
90%，R@1 从 0.310 走到 0.478 —— 它们不是拿更多误报换来的，
而是修掉了「一个词没对上就整条不命中」。

更松的两格（0.336 / 0.398）代价是 80% / 70%，留着，**等 abstention 能上生产再取**。

### 6.4 一个指标口径的错误

`ambiguous` 那一类的 R@1 曾经是 **0.000，而且是结构性的**：
一条要求两篇笔记的用例，在 Top-1 的列表里**永远**不可能满足。
把它算成 miss，测到的是指标定义而不是系统表现。

改为 **Recall@K 的分母只包含 `expected.count ≤ K` 的用例**。
10 条歧义用例不排除的话，会让每一条臂的 R@1 一律低约 0.05 ——
而**绝对下限那一行会因此判错**。K=3 / K=5 时它们仍然进分母，严格口径不变。

配对 bootstrap 用**同一个分母**（`EvalCaseOutcome.participates(in:)`）：
两边分母不一致时，「置信区间」算的就不是那个点估计的区间。

### 6.5 四条被修正的旧断言

它们都把**缺陷写成了设计前提**：

1. 「整句自然语言 query 在 keyword 路零命中（交给语义路）」——
   改为断言现在会命中且带高亮，并用**跨语言** query 重新覆盖「整页零高亮」那一屏
   （那才是它的真实来源）。
2. 同上，`SearchPresentationChecks` 的 §2.6 说明行。
3. 「keyword 对无答案 query 克制率恰好 100%」—— 100% 是分词缺陷的副产品：
   一个从不匹配中文自然句的词法路当然永远返回空。它测的不是「会克制」，
   而是「查不动中文」。改为 ≥ 0.85。
4. `EvalChecks` 的严格口径用例依赖「检索找不到 noodle」，切分改进后 noodle
   被找到了。改用一个**不存在的 note id**，把检索行为这个变量消掉，
   并补一条反向断言。

---

## 7. 回归集：一个棘轮，不是愿望清单

Gate 的第四行读回归集的通过率。**集合为空时它恒过，等于没有那一行。**

第一版把 12 条「两个缺陷的证人」全放了进去，跑出来只有 7 条当前通过 ——
Gate 的第四行变成 42.9%，永久红。**那不是回归，是还没做到。**
一个永远红的 Gate 会被忽略，而这正是「一个总是被跳过的 Gate 等于没有 Gate」。

所以回归集只保留**当前通过**的 7 条，覆盖 4 个类别。
剩下 5 条归入 **FUTURE RETRIEVAL RESEARCH**（`PROJECT_CLOSURE.md`）——
它们不是回归，也不是待办，是**已知的排序能力边界**：

```
HG041  搜索功能到底什么时候能给内部测试，为什么没上线   两个来源都要召回
LT01   会议里说搜索这周不上线，那到底哪天发             词面陷阱，最难的一类
CR01   上次老师说那个不能退课之前要先做什么             query 与答案零词面重合
NQ04   南门停车费 一小时几块 上限多少                   电报式，全是虚词
NQ11   whats my ded again for in network               ded 是口语缩写
```

`swift run mosaic-checks` 每次都打印「回归集点名」并断言全部通过。
**有一条掉了就是真的回归**，那时该查代码，不是改名单。

---

## 8. 还缺什么才能真的上线

| # | 缺口 | 谁能做 | 状态 |
|---|---|---|---|
| 1 | **评测集仍是 agent 编写的场景化用例** | 用户：真实笔记 + 人工编写的 query | ⏭ **唯一的实质阻断项** |
| 2 | **绝对下限是 provisional** —— 依据是第 1 条那份数据 | 拿到第 1 条之后重新校准 | ⏭ 跟着第 1 条 |
| 3 | **付费 Apple Developer 账号** —— TestFlight / App Store Connect / CloudKit | 用户 | 🔒 EXTERNAL |
| 4 | **`lexical_trap` 只有 0.095** —— 这一类正是语义路存在的理由 | 工程 + 云端臂 | 🔬 研究，不阻塞 |
| 5 | **TD-10 相关性下限** —— local-hybrid 负例克制率 0% 的直接原因 | 工程 | 🔬 研究，不阻塞 |
| ~~云端臂未跑~~ | ⏸ **DEFERRED**（产品决策）。`SKIPPED — CREDENTIALS NOT CONFIGURED` 是它的正常状态，不是失败 | — | ⏸ |
| ~~延迟未在真机复测~~ | ✅ **已完成**（§5c）。真机 Release 上冷热两侧都在预算内 | — | ✅ |

第 1 条最重要：**Gate PASS 只说明判定链路闭合，不说明真实用户体验达标。**

---

## 9. 这一轮 Gate 自己被改进的地方

**Gate 的可信度取决于它有没有真的跑过。** 本轮发现两处「跑过但没保护到东西」：

1. **`MosaicBench` 的 7 条一直在 skip。** 设计上要求真机，而此前没有真机 ——
   于是延迟那一整条从来没有产生过结论。真机跑起来之后，
   **第一次就 FAIL**（Metric A 冷 P50 103.01 ms > 100 ms 预算）。
   一个从来没跑过的 Gate 与没有 Gate 没有区别。

2. **12 条 Gate 相关的单测写死 `requiredDeviceClass: .simulator`。**
   它们在真机上全部判 STALE —— 也就是说，「Promote 的前置校验」这件事
   在真机上从来没有被验证过。已改成取当前机器的实际环境。

新增的第三件：**`MosaicBench.test7` 逐条点名五个已修缺陷的证人**，
跑在生产 keyword 配置上。`test6` 出的是 60 条 holdout 的汇总，
一条用例翻面只值 0.017 —— 中文长 query 的修复要是又坏了，汇总数字上看不出来。
