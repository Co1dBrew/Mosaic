# 「万象记 / Mosaic」AI 多媒体笔记 App · 产品需求文档(PRD)

> 产品名:**万象记(Mosaic)**。本版已整合首轮确认的核心设计决策。

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.2(已确认核心决策) |
| 文档状态 | 主要决策已敲定,细化中 |
| 平台 | iOS(iPhone 优先,iPad/Mac 后续) |
| 作者 | Qingdong（与 Claude 共同整理) |
| 日期 | 2026-06-15 |
| 本版主要变更 | ①产品定名 Mosaic;②确认采用块状线性文档;③确认 AI 总结整张卡片;④图片纳入 AI 视觉理解;⑤新增「增量更新总结」机制(保留初始总结,只总结改动);⑥iCloud 同步纳入 MVP。详见第 11 章。 |

---

## 1. 产品概述

### 1.1 一句话定位
一款「什么都能往里塞」的多媒体笔记 / 日记 App:每条笔记可混合放入**文字、语音、图片、文档和链接**;在列表里每条笔记折叠成一个长条,点开小三角即可看到一张由 AI 生成的「内容总结贴纸」,快速知道这条笔记记了些什么;内容更新后,AI 还会自动补一条「这次改了什么」的更新记录。

### 1.2 产品愿景
让"随手记录"零负担——想到什么、看到什么、说点什么,都丢进同一张笔记里,不纠结格式;再靠 AI 自动归纳,把杂乱的记录变成一眼看懂的摘要,并持续跟踪它的变化。

### 1.3 核心价值
- **多模态混排**:一张笔记里文字 / 录音 / 图片 / 文档 / 链接自由组合。
- **低门槛输入**:支持现场拍照、现场录音(允许"想到哪说到哪"的零散口述)、直接打字。
- **AI 自动总结 + 增量跟踪**:把整张笔记(含语音转写、文档文字、图片画面)交给大模型按固定格式生成摘要;内容变更后自动追加"本次更新"摘要,旧总结保留。
- **自带 Key、可换模型**:内置 Kimi、DeepSeek,并支持自填任意兼容 OpenAI 接口的服务(DIY)。

### 1.4 目标用户
- 嫌备忘录"只能放文字"的人;需要把语音、截图、PDF、链接攒在一起、事后快速回顾的学生 / 研究者 / 内容工作者;爱用语音速记灵感、又懒得整理的人。

### 1.5 典型使用场景
- 开会:拍白板、录要点、打待办、贴文档链接 → AI 总结成纪要;会后补充几条 → 自动追加"本次更新"。
- 读资料:导入 PDF、写想法、贴原文链接 → AI 说明这条笔记围绕什么、涉及哪些点。
- 生活日记:拍照 + 语音碎碎念 + 几句文字 → AI 归纳"这天主要记了什么"。

---

## 2. 名词解释(Glossary)

| 名词 | 含义 |
|---|---|
| **文件夹(Folder)** | 顶层分类容器,用户自建,类似"项目 / 种类"。 |
| **笔记卡片(Card / Note)** | 文件夹里的一条记录,核心单元;内部可混排多种内容块。 |
| **内容块(Block)** | 卡片内部的一段内容,类型:文字 / 图片 / 录音 / 文档 / 链接。 |
| **展开态(Editor)** | 进入卡片后的"块状线性编辑页"。 |
| **折叠态(Collapsed Bar)** | 列表里每张卡片折叠成的长条,带展开摘要的小三角。 |
| **AI 摘要贴纸(Summary Sticker)** | 点小三角展开的贴纸,含「初始总结」与「更新记录」。 |
| **初始总结(Base Summary)** | 卡片首次生成的整体总结,长期保留。 |
| **更新记录(Update Log)** | 每次内容变更后自动追加的"本次改了什么"摘要列表(倒序)。 |
| **AI 服务商(Provider)** | 生成摘要调用的大模型服务,内置 Kimi、DeepSeek,另支持自定义。 |

---

## 3. 信息架构与页面结构

```
App
├── 首页 / 文件夹列表页
│   ├── [文件夹 A] / [文件夹 B] / …
│   └── [＋ 新建文件夹]
│
├── 文件夹详情页(卡片列表,默认折叠态)
│   ├── [卡片长条 1]  ▸小三角 → AI 摘要贴纸(初始总结 + 更新记录)
│   └── [＋ 新建卡片]
│
├── 卡片编辑页(展开态 · 核心 · 块状线性文档)
│   └── 内容块流:文字 / 图片 / 录音 / 文档 / 链接 …
│       └── [＋ 添加内容](拍照 / 选图 / 录音 / 导入文档 / 加链接 / 文字)
│
└── 设置页
    ├── AI 服务商与 API Key / 默认模型
    ├── 摘要设置(自动更新开关等)
    ├── 同步与数据(iCloud)
    ├── 隐私 / 关于
```

导航以栈式"进入 + 返回"为主,符合 iOS 习惯。

---

## 4. 功能需求详述

> 优先级:**P0 = MVP 必做**;**P1 = 重要,二期**;**P2 = 增强,后续**。

### 4.1 文件夹管理(P0)
- 新建 / 重命名 / 删除文件夹;删除二次确认,提示"将同时删除其中 N 张卡片"。
- 每个文件夹可设**名称 + 图标 / 颜色**;列表显示名称 + 卡片数;拖动排序(P1)。
- 空状态引导("点击 ＋ 写下第一条")。

### 4.2 笔记卡片管理(P0)
- 文件夹内新建 / 删除卡片;删除二次确认。
- 卡片标题:用户可填;未填则由 AI 总结自动生成(见 4.8)。
- 记录创建 / 最后修改时间;默认按"最后修改时间倒序"。
- 置顶(P1)、移动到其它文件夹(P1)、搜索 / 标签(P1,见 4.9)。

### 4.3 卡片编辑器(展开态,核心)(P0)
**块状线性文档**:内容从上到下竖向堆叠(类似备忘录 / Notion),非自由画布(已确认)。

底部 / 行内提供**「＋ 添加内容」**入口,可选:文字 / 拍照 / 从相册选图 / 录音 / 导入文档 / 添加链接。

通用能力:各块可拖动排序、删除;长按出操作菜单;编辑自动保存;全程可离线编辑(仅 AI 总结需联网)。

#### 4.3.1 文字块(P0)
- 轻量富文本:加粗 / 斜体 / 有序无序列表 / 标题 / 分割线(若工期紧,MVP 可先纯文本;见第 11 章默认项)。
- 文中网址自动识别可点。

#### 4.3.2 录音块(P0)
- 现场录音:开始 / 暂停 / 继续 / 停止;显示时长与简单波形。
- 本地保存,可回放、拖动进度、删除。
- **语音转写(STT)**:每段录音生成转写稿,供回看与 AI 总结。建议用 **iOS 端上语音识别(Speech 框架)**(免费、可离线、不外传音频)。转写稿可展开查看与手动修正。
- 允许长段零散口述,转写不要求标点完美,交给 AI 归纳。

#### 4.3.3 图片块(P0)
- 来源:现场拍照 或 相册多选导入。
- 缩略图展示,点击全屏 / 缩放;原图存本地沙盒。
- **图片参与 AI 总结**:启用支持视觉的模型时,图片会作为视觉输入随卡片一起发给模型,让 AI"看图"理解内容(已确认);用户也可为图片选填说明(caption)辅助理解。
- 发送前自动**压缩 / 缩放**(如长边 ≤1568px、转 JPEG),控制流量、token 与费用;单次纳入的图片数量设上限(超出时取较近的若干张,或提示)。

#### 4.3.4 文档块(P0)
- 系统选择器导入,至少支持 **PDF、Word(.doc/.docx)**;其余格式(txt、ppt、xlsx 等)P1 逐步支持。
- 显示文件图标 + 文件名 + 类型;点击在 App 内预览(QuickLook)。
- **正文文字提取**(供 AI 总结):PDF 用 PDFKit;Word 需解析或转换(有工作量,见技术建议)。提取文字不展示给用户。
- 超长文档处理见第 11 章默认项(MVP 截断 + 提示)。

#### 4.3.5 链接块(P0)
- 输入 / 粘贴网址生成链接块;点击可跳转打开(默认 App 内浏览器 SFSafariViewController,长按可"用 Safari 打开 / 复制")。
- 链接预览卡片(标题 / 图标 / 缩略图)(P1,LinkPresentation);MVP 先显示网址 + 标题。
- 链接标题 / 描述作为 AI 总结输入之一。

### 4.4 卡片折叠态(列表长条)(P0)
文件夹详情页每张卡片默认折叠为长条,显示:标题(用户填或 AI 生成)、一句话概述(来自初始总结 `one_liner`)、内容类型小图标行(🎙️🖼️📄🔗)、最后修改时间、末尾**小三角 ▸**。
- 点长条主体 → 进入编辑器(展开态)。
- 点小三角 ▸ → 展开 / 收起 AI 摘要贴纸(见 4.5)。
- 若有未读的"更新记录",小三角旁加一个小红点 / 数字提示。

### 4.5 AI 摘要贴纸 与 增量更新总结(P0)★本版重点
点击小三角后展开的贴纸分两部分:

**A. 初始总结(Base Summary)——长期保留**
首次生成,内容按固定结构(对应 5.1 Prompt 输出):
- 内容类型 / 主题标签(如:会议记录 / 读书笔记 / 灵感速记);
- 主题关键词 chips(3–6 个);
- 要点列表(3–8 条):这条笔记都提到了哪些内容;
- 总结段落(80–150 字):整体记录了哪些事、围绕什么展开;
- 底部:所用模型 + 生成时间。

**B. 更新记录(Update Log)——倒序追加,旧条目保留**
卡片内容发生变更后**自动**生成一条"本次更新"摘要,追加到列表顶部,**不覆盖**初始总结,也不覆盖此前的更新条目。每条包含:
- 更新时间;
- 一句话:这次主要改了什么(`update_one_liner`);
- 变更要点(1–6 条):本次**新增 / 修改 / 删除**了哪些内容(`changes`)。

这样贴纸读起来就是:**「这条笔记整体讲了什么(初始总结) + 它后来怎么一步步变化的(更新记录)」**。

**触发与生成逻辑:**
- **首次**点开小三角 → 生成「初始总结」(loading 骨架屏),缓存。
- 之后卡片被编辑并满足触发条件 → **自动**生成一条「更新记录」(默认开启,可在设置关闭;见下)。触发采用**防抖**:在退出编辑器、或停止编辑一段时间后才触发,且仅当变更非微小(避免频繁调用、控制费用)。
- 变更检测(块级):App 为每次总结保存一份卡片快照(各内容块的 ID + 内容指纹 hash);再次总结时对比当前与快照,得出**新增块 / 内容改动块 / 删除块**,组成"变更集"发给模型(只发改动部分,不重发全文,省 token)。生成后更新快照。
- **手动操作**:贴纸提供「立即更新」(强制生成一条更新记录)与「重新生成完整总结」(重做初始总结,并将旧的初始总结+更新记录归档或清空)。
- **失败 / 边界**:网络 / Key 无效 / 额度不足 → 明确错误 + 重试 / 去设置入口;内容过少 → 提示"内容太少,暂无可总结的内容";变更过小 → 跳过本次自动更新。

**设置项**(P0):`内容变更后自动追加更新总结 [开 / 关]`(默认开)。

### 4.6 AI 总结的输入聚合(P0)
点击 / 触发总结时,App 在本地组装请求:

**初始总结**——把整张卡片组装为一次(多模态)请求:
1. 卡片标题(若有);
2. 所有文字块文本(按顺序);
3. 各录音块转写稿;
4. 各文档块提取的正文文字(可能按需截断);
5. 各链接块的网址 + 标题 + 描述;
6. 各图片:作为**视觉输入**随消息发送(模型支持视觉时);用户填写的图片说明一并附上。

**更新总结**——只组装"变更集":新增 / 改动的文字、转写、文档文字、链接、新增图片(视觉),并附上"此前的初始总结文本"作为上下文,让模型聚焦改动(见 5.2 更新 Prompt)。

> 多模态消息格式见 5.4;若所选模型不支持视觉,则图片退化为"仅用说明文字 / 跳过图片"。

### 4.7 设置 — AI 服务商与 API Key(P0)
- **服务商选择**(单选):**Kimi(Moonshot)**(内置)、**DeepSeek**(内置)、**自定义(DIY,兼容 OpenAI Chat Completions 的任意服务)**。
- **API Key 输入**:用户填自己的 Key;**安全存于 iOS Keychain**,不明文落盘、不上传我方服务器。
- **模型选择**:每个服务商给推荐模型下拉(可手填覆盖)。开发时以官方最新文档为准,建议默认(见第 7 章):
  - Kimi:`kimi-k2.6`(支持视觉,适合图片理解);
  - DeepSeek:`deepseek-v4-flash`(快/省)/ `deepseek-v4-pro`(强)——**视觉能力需开发时核实,若不支持图片输入则对图片走退化方案**;
  - 自定义:填 **Base URL + 模型名 + API Key**。
- **「测试连接」按钮**:发最小请求验证可用。
- 文案:调用费用用户自付 + 隐私说明(见 4.10、第 6 章)。

### 4.8 AI 自动生成标题(P1,可选 P0)
卡片未命名时,初始总结顺带产出 ≤20 字标题(Prompt 已含 `title`),用于折叠态显示;用户可随时手动覆盖。

### 4.9 搜索与标签(P1)
- 搜索命中:卡片标题、文字内容、转写稿、AI 摘要。
- 标签:可给卡片打标签并筛选;AI 关键词一键转标签(P2)。

### 4.10 同步、隐私与数据(P0)
- **iCloud 同步**(已确认纳入 MVP):文件夹 / 卡片 / 内容块 / 媒体文件在用户的多台设备间同步(技术见第 7 章 CloudKit)。
- **隐私告知**:首次使用 AI 总结前弹窗说明——点击总结会把该卡片的**文字内容与图片**发送到你在设置中选择的第三方 API;原始录音默认不发送(以本地转写文字代替)。
- 提供清除缓存 / 清除某卡片 AI 摘要(含更新记录)的能力。

---

## 5. AI 总结 Prompt 设计(关键交付物)

> 目标:无论卡片里放了什么,都按**固定 JSON 结构**输出,便于 App 稳定解析渲染。两家内置服务及多数自定义服务均兼容 OpenAI Chat Completions 的 `response_format: {"type":"json_object"}`,建议开启,并在提示词中明确"仅输出 JSON"。

### 5.1 初始总结 · System Prompt

```
你是一个「笔记内容总结助手」。用户会给你一篇「笔记卡片」的全部内容。这些内容可能由多种形式拼接而成:用户手打的文字、语音转写得到的文字记录、文档(PDF / Word)中提取的文字、网页链接的标题与描述,以及随消息附带的图片。请你通读全部文字并查看图片,判断这篇笔记到底记录了什么,并严格按下面指定的 JSON 格式输出一份总结。

总结要求:
1. 只依据给定内容(含图片所见)进行总结,不要编造内容里没有出现的信息;某字段缺乏依据时,数组字段输出 []、文本字段输出"信息不足"。
2. 语言与笔记内容保持一致:中文内容用简体中文,英文内容用英文,混合时以主要语言为准。
3. 语音转写文字可能口语化、零散、有口误,请抓住核心意思,忽略明显的语气词与重复。
4. 图片请描述其与笔记主题相关的关键信息(画面内容、文字、图表等),不要逐像素描述无关细节。
5. 输出必须是一个合法的 JSON 对象,不要输出任何额外解释、前言、结语,也不要包裹 Markdown 代码块标记(不要出现 ``` )。

请严格按以下 JSON 结构输出(字段名固定,不要增减):
{
  "title": "为这篇笔记拟一个简洁标题,不超过 20 个字",
  "one_liner": "用一句话概括这篇笔记主要在讲什么,不超过 40 个字",
  "type": "内容类型/性质,例如:会议记录 / 读书笔记 / 学习资料 / 灵感速记 / 旅行随记 / 工作待办 / 生活日记 等,只给一个最贴切的",
  "topics": ["涉及的关键主题或关键词,3 到 6 个,每个为简短词组"],
  "key_points": ["逐条列出笔记里提到的主要内容或要点,3 到 8 条,每条是一句完整的话"],
  "summary": "用一段连贯通顺的话对整篇笔记做总体总结,80 到 150 字,说明它整体上记录了哪些事、围绕什么展开"
}
```

### 5.2 增量更新 · System Prompt

```
你是「笔记更新总结助手」。用户已有一篇笔记卡片,之前已生成过整体总结;现在这张卡片有了新的修改。下面会给你:(A) 这张笔记之前的整体总结;(B) 本次相对上次的变更内容,标注了新增 / 修改 / 删除,可能含新增的图片。请你只针对【本次的变更】简要说明这次更新增加或改动了什么,不要复述 (A) 里已有的旧内容。

要求:
1. 只总结本次变更,不复述未改动的旧内容;若有删除,简要说明删掉了什么。
2. 语言与内容一致;新增图片请说明其关键信息。
3. 严格输出合法 JSON,无额外文字、无代码块标记。

请严格按以下 JSON 结构输出:
{
  "update_one_liner": "用一句话概括本次更新主要改了什么,不超过 30 个字",
  "changes": ["逐条列出本次新增/修改/删除的要点,1 到 6 条,每条一句话"]
}
```

### 5.3 User Prompt

**初始总结:**
```
以下三引号之间是这篇笔记卡片的全部文字内容(图片随本消息附带),请按要求总结并仅输出 JSON:
"""
{{aggregated_card_content}}
"""
```

**增量更新:**
```
(A) 这篇笔记之前的整体总结:
"""
{{previous_summary_text}}
"""
(B) 本次相对上次的变更内容(新增/修改/删除,新增图片随本消息附带):
"""
{{change_set_text}}
"""
请仅输出 JSON。
```

### 5.4 多模态(图片)消息格式
含图片时,`messages` 中 user 消息的 `content` 用数组,混合文字与图片(OpenAI 兼容写法):
```json
{
  "role": "user",
  "content": [
    { "type": "text", "text": "<上面的 User Prompt 文字>" },
    { "type": "image_url", "image_url": { "url": "data:image/jpeg;base64,<图片1>" } },
    { "type": "image_url", "image_url": { "url": "data:image/jpeg;base64,<图片2>" } }
  ]
}
```
- 仅在所选模型支持视觉时使用(Kimi K2.x 系列支持;DeepSeek 视觉能力开发时核实)。
- 图片发送前压缩 / 缩放、限制张数,控制 token 与费用。
- 不支持视觉时:去掉图片项,改用图片说明文字,或跳过图片。

### 5.5 输出示例

**初始总结:**
```json
{
  "title": "周三产品评审会要点",
  "one_liner": "记录了本周产品评审会的主要结论与待办分工。",
  "type": "会议记录",
  "topics": ["产品评审", "版本排期", "待办分工", "AI 摘要功能"],
  "key_points": [
    "确认下个版本优先做卡片折叠态与 AI 摘要贴纸。",
    "录音转写先用系统端上识别,后续再评估云端方案。",
    "设计稿周五前给出折叠态长条的视觉规范。"
  ],
  "summary": "这篇笔记记录了周三的产品评审会内容,主要围绕下个版本的功能取舍与排期展开,明确了卡片折叠态、AI 摘要贴纸为优先项,并就语音转写方案与设计交付时间做了分工。整体是一份可直接转为执行清单的会议纪要。"
}
```

**增量更新:**
```json
{
  "update_one_liner": "新增了 iCloud 同步的排期,并删掉了云端转写方案。",
  "changes": [
    "新增:iCloud 同步纳入首个版本,负责人本周给方案。",
    "修改:AI 摘要改为支持图片视觉理解。",
    "删除:移除了原先的云端语音转写计划。"
  ]
}
```

### 5.6 调用参数建议
- `temperature`:0.2–0.3(稳定结构化输出;**Kimi/Moonshot 的 temperature 范围为 [0,1]**,勿超 1)。
- `response_format`:`{"type":"json_object"}`(DeepSeek 文档要求 JSON 模式下提示词含 "json" 字样,本 Prompt 已满足;不支持该参数的自定义服务则关闭,仅靠提示词约束)。
- `max_tokens`:留足(初始 ~1024,更新 ~512),避免 JSON 截断。
- 解析:`trim`、去除可能的 ```json 围栏,再 `JSON.parse`;失败则提示重试。

---

## 6. 非功能性需求

### 6.1 隐私与安全(重点)
- **API Key**:存 iOS Keychain,不明文落盘、不经我方服务器(本产品无后端,设备直连所选服务商);可选随 iCloud Keychain 同步到用户自己的设备。
- **内容数据**:笔记 / 录音 / 图片 / 文档存本地沙盒,并通过 iCloud(用户私有库)在其本人设备间同步;默认不上传我方服务器。
- **AI 总结的数据出境**:点击 / 触发总结时,会把**文字内容与图片**经 HTTPS 发送给用户所选服务商;原始录音默认不发送(用本地转写文字代替)。首次使用前弹窗告知并征得同意。
- **直连模式说明**:每个用户用自己的 Key,设备直连第三方 API 合理;若将来改用"我方统一 Key",必须改为经我方后端代理。

### 6.2 离线能力
- 文件夹 / 卡片创建编辑、录音、端上转写、图片、文档预览、链接保存全部可离线;仅 AI 总结需联网,离线时给明确提示。iCloud 同步在联网后自动进行。

### 6.3 性能
- 图片 / 文档缩略图 + 懒加载控制内存;录音、转写、图片压缩、AI 请求均异步不阻塞主线程;AI 请求带超时与重试。

### 6.4 兼容性与适配
- 目标系统:建议 **iOS 16+**(端上识别、SwiftData/CloudKit 等较成熟;下限按团队评估)。
- 设备:iPhone 优先;适配深色模式、动态字体。

### 6.5 可访问性
- VoiceOver、Dynamic Type;各内容块提供无障碍标签。

---

## 7. 技术实现建议(iOS)

> 建议性质,非约束。API 信息核对于 2026-06,**开发时以各服务商官方文档为准**。

### 7.1 端侧技术栈
- **语言 / UI**:Swift + SwiftUI(必要处混 UIKit)。
- **本地存储 + 同步**:**SwiftData + CloudKit** 或 Core Data + CloudKit,实现本地持久化与 iCloud 私有库同步;媒体文件经 CloudKit Asset 同步;冲突按 CloudKit 机制 / 后写覆盖处理。
- **录音 / 播放**:AVFoundation(AVAudioRecorder / AVAudioPlayer)。
- **语音转写(STT)**:Speech 框架(SFSpeechRecognizer),端上离线识别(中英文)。
- **拍照 / 选图**:相机 AVCapture / UIImagePickerController;相册多选 PHPickerViewController。
- **文档导入 / 预览**:UIDocumentPickerViewController 导入;QuickLook 预览。
- **文档文字提取**:PDF 用 PDFKit;Word(.docx 为 zip+XML)需解析库或转换(**有工作量**,MVP 可先稳妥支持 PDF + txt,Word 二期完善)。
- **链接预览 / 打开**:LinkPresentation;SFSafariViewController。
- **图片压缩**:导出前缩放(长边 ≤1568px)、JPEG 压缩,降低发送体积与 token。
- **网络**:URLSession 构造 OpenAI 兼容 `/chat/completions` 请求(支持多模态 content 数组)。
- **安全存储**:Keychain Services 存 API Key。
- **富文本**:AttributedString / 富文本编辑组件(若做轻量富文本)。
- **变更检测**:为每张卡片维护"已总结快照"(块 ID → 内容 hash),用于生成更新总结的"变更集"。

### 7.2 内置 AI 服务接入要点(均兼容 OpenAI Chat Completions)
| 服务商 | Base URL | 推荐模型 | 视觉 | 备注 |
|---|---|---|---|---|
| **DeepSeek** | `https://api.deepseek.com`(或 `/v1`),端点 `/chat/completions` | `deepseek-v4-flash`(快/省)、`deepseek-v4-pro`(强) | 需核实 | 兼容 OpenAI/Anthropic;支持 JSON 模式;旧名 `deepseek-chat`/`deepseek-reasoner` 计划 2026-07-24 下线 |
| **Kimi(Moonshot)** | `https://api.moonshot.ai/v1`,端点 `/chat/completions` | `kimi-k2.6`(通用/多模态) | 支持 | 兼容 OpenAI SDK;Base URL 分区域,以控制台为准;temperature 范围 [0,1] |
| **自定义(DIY)** | 用户填写 | 用户填写 | 视模型 | 任意兼容 OpenAI Chat Completions 的服务,需填 Base URL + 模型名 + Key |

统一请求体(示意,含多模态):
```json
{
  "model": "<所选模型>",
  "temperature": 0.2,
  "response_format": { "type": "json_object" },
  "max_tokens": 1024,
  "messages": [
    { "role": "system", "content": "<5.1 或 5.2 的系统提示词>" },
    { "role": "user", "content": [
        { "type": "text", "text": "<5.3 的用户提示词>" },
        { "type": "image_url", "image_url": { "url": "data:image/jpeg;base64,<图片>" } }
      ] }
  ]
}
```
鉴权:HTTP 头 `Authorization: Bearer <用户 API Key>`。

---

## 8. 数据模型(建议)

```
Folder
  id, name, iconOrColor, sortOrder, createdAt, updatedAt

Card
  id, folderId, title?, isPinned, createdAt, updatedAt
  blocks: [Block]              // 有序
  aiSummary?: AISummary

Block (按 type 区分 payload)
  id, cardId, order, type ∈ {text, image, audio, file, link}, contentHash
  - text:  { richText / markdown }
  - image: { localPath, thumbnailPath, source ∈ {camera, library}, caption? }
  - audio: { localPath, durationSec, transcript?, waveform? }
  - file:  { localPath, fileName, fileType, thumbnailPath?, extractedText? }
  - link:  { url, title?, description?, faviconPath? }

AISummary
  cardId
  base: {                      // 初始总结(长期保留)
    title, oneLiner, type, topics[], keyPoints[], summary,
    modelUsed, providerUsed, generatedAt
  }
  updateLog: [                 // 更新记录(倒序追加,旧条目保留)
    { updateOneLiner, changes[], modelUsed, generatedAt }
  ]
  lastSnapshot: { [blockId]: contentHash }   // 变更检测基线

Settings
  provider ∈ {kimi, deepseek, custom}
  modelName
  customBaseUrl?               // provider=custom 时
  autoUpdateSummary: Bool      // 内容变更后自动追加更新总结,默认 true
  hasAcceptedAIPrivacyNotice: Bool
  iCloudSyncEnabled: Bool      // 默认 true
  // apiKey 不入库,存 Keychain
```
> 以上结构通过 SwiftData/Core Data + CloudKit 同步(媒体走 CloudKit Asset);`lastSnapshot` 与 Keychain 中的 Key 可选择不参与跨设备同步。

---

## 9. 关键交互流程(文字版)

**新建卡片 + 初始总结 + 后续更新**
1. 首页 → 进入/新建文件夹 → ＋ 新建卡片 → 编辑器(展开态)。
2. 「＋ 添加内容」逐块添加(拍照/录音/文字/文档/链接)。录音停止自动转写;文档导入后台提取文字。
3. 返回列表,卡片以折叠态长条显示。
4. 点小三角 ▸ → 首次触发生成**初始总结**(loading) → 展开贴纸看到主题/要点/总结。
5. 再次编辑该卡片(如补一段录音、加一张图) → 退出编辑后自动生成一条**更新记录**,追加到贴纸顶部,旧总结保留;长条小三角旁出现红点提示。
6. 需要时点「重新生成完整总结」重做初始总结。

**首次配置 AI**
1. 设置 → 选服务商 → 填 Key(自定义时填 Base URL、模型名) → 测试连接 → 成功后可用。
2. 第一次点总结时弹隐私确认 → 同意后调用。

---

## 10. MVP 范围与迭代规划

### 10.1 MVP(P0,首个可用版本)
- 文件夹:增删改、图标/颜色、列表。
- 卡片:增删、标题、按修改时间排序、折叠态长条。
- 编辑器(块状线性文档):文字 + 录音(录/放/端上转写) + 图片(拍照/相册) + 文档(导入/预览/PDF 文字提取) + 链接(保存/打开)。
- AI 摘要:整张卡片初始总结 + **图片视觉理解** + **内容变更自动追加更新总结(保留初始总结)** + 固定 JSON Prompt + 解析 + 贴纸渲染(初始总结 + 更新记录) + 缓存 + 手动重做。
- 设置:服务商 / Key(Keychain)/ 模型 / 测试连接 / 自动更新开关 / 隐私告知。
- **iCloud 同步**(文件夹/卡片/内容块/媒体)。
- 本地存储、离线编辑、错误处理。

> 说明:**图片视觉理解、增量更新总结、iCloud 同步** 三项工作量相对较大,若进度紧张可按此顺序作为首个版本内的"先后落地"项(均仍属 P0,只是排序)。

### 10.2 二期(P1)
- 搜索、标签、置顶、跨文件夹移动、拖动排序。
- 轻量富文本、链接预览卡片、图片说明完善。
- 分享 / 导出(单卡片导出为文本 / PDF)。
- Word(.docx)文字提取完善。

### 10.3 后续(P2)
- 画图 / 手写涂鸦块(本次先去掉,后续再加)。
- 跨卡片 / 整文件夹汇总总结。
- 提醒、桌面小组件、模板、协作 / 分享给他人。

---

## 11. 决策状态汇总

### 11.1 已确认的核心决策
| # | 议题 | 结论 |
|---|---|---|
| 1 | 卡片排版 | **块状线性文档**(非自由画布) |
| 2 | AI 总结范围 | **整张卡片**(非单个文档) |
| 3 | 图片是否参与 AI | **参与**,用支持视觉的模型让 AI 看图 |
| 4 | 内容变更后是否重新总结 | **自动重新总结,但保留初始总结,只生成"本次改了什么"的更新记录**(见 4.5 增量更新机制) |
| 5 | iCloud 同步 | **纳入 MVP** |
| 6 | 产品命名 | **万象记 / Mosaic** |

### 11.2 仍按"建议默认"执行(如需调整请提出)
| # | 议题 | 默认采用 |
|---|---|---|
| a | 语音处理 | iOS 端上转写(免费/可离线/不外传音频),保留原始录音可回放 |
| b | 文字格式 | 轻量富文本(工期紧可降级为纯文本) |
| c | 隐私弹窗 | 首次使用 AI 总结前弹窗告知(文字+图片会发送给第三方) |
| d | 超长文档 | MVP 先截断 + 提示;分段汇总(map-reduce)留后续 |
| e | 平台范围 | 先 iPhone,iPad/Mac 后续 |

---

## 附录 A:命名(已定)
- **正式名:万象记(Mosaic)** —— 贴合"什么都能塞进一张卡片"的多模态核心卖点。
- 其余候选(备查):絮记 / Murmur、拾光 / Glean、灵犀 / Insight、随记 / Jotter。

## 附录 B:后续可补充
- 视觉规范(折叠态长条、摘要贴纸的初始总结 + 更新记录两段式布局、配色、图标)——可另出 UI 设计稿。
- 埋点 / 指标(留存、AI 总结使用率、自动更新触发率与成本等)。
- App Store 隐私清单(Privacy Manifest)与权限文案(麦克风、相机、相册、语音识别、网络)。
- 成本与限流策略(单卡片图片张数上限、文档截断阈值、自动更新防抖时长等具体数值)。

---

*本版已敲定核心方向。确认 11.2 的默认项后即可定稿,进入设计 / 开发。*