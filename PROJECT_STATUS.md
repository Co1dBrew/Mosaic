# Mosaic · 项目现状

> **这份文件回答「现在到底是什么状态」。** 计划与历史在别处（`design/`、`HANDOFF.md`）。
> 一份同时描述「打算怎么做」和「现在怎么样」的文档一定会过期，所以这两件事分开写。
>
> 最后更新：2026-09-01 · 本轮新增的是**真机验证**

---

## 0. 一句话

```
ENGINEERING COMPLETE
DEVICE VALIDATED

PRODUCT VALIDATION PENDING   人工编写的评测 query
DEFERRED                     云端语义检索（产品决策）
EXTERNAL                     App Store / TestFlight 账号配置
```

产品（UI v2）与检索质量平台都已落地、有测试守着、**并且在一台真实 iPhone 上验证过**。
生产检索是 **keyword**，代码、配置、UI、Gate、文档现在说的是同一件事 ——
这一轮之前不是（见 §9）。

---

## 1. 发布就绪度

**「code-ready」与「account-ready」是两件事，分开写。**

| 目标 | 代码侧 | 账号侧 | 结论 |
|---|---|---|---|
| **Internal Beta**（内部装机试用） | ✅ 就绪 | ✅ 个人 Team 够用 | ✅ **可以** |
| **External Beta / TestFlight** | ✅ 就绪 | ❌ 需要付费 Apple Developer | ⚠️ **code-ready，account 未就绪** |
| **App Store** | ✅ 就绪 | ❌ 同上 | ❌ **不可以**（另见 B1） |

### 阻断项

| # | 阻断什么 | 谁能解 |
|---|---|---|
| **B1** | **评测集不是真实用户数据**：`provenance = agent_authored_realistic`。Gate PASS 只说明判定链路闭合，**不说明真实用户体验达标** | 用户：真实笔记 + 人工编写的 query |
| **B2** | **付费 Apple Developer 账号**：TestFlight / App Store Connect / CloudKit 容器都要它。免费个人 Team 还有一条硬限制 —— **一台设备最多 3 个 App**，本轮真机 UI 测试就先撞到过它 | 用户 |

**云端语义检索不再是阻断项** —— 产品决策已把它标为 `DEFERRED`（§3）。

---

## 2. 产品（Notes App）

| | 状态 |
|---|---|
| 信息架构 | ✅ UI v2：**2 级**（笔记流 → 笔记页）。旧的 3 级已删除 |
| 首页 | ✅ 文件夹 chip 筛选 · 笔记行 · 滑动与长按 · 悬浮新建 |
| 笔记页 | ✅ 摘要条（44pt，原地展开）· 文件夹选择器 · `⋯` 收口 · 底部插入工具条 |
| 内容块 | ✅ 文字 · 图片 · 录音（转写直显）· 文档 · 链接 |
| 移动到文件夹 · 文件夹拖动排序 | ✅ |
| 搜索 | ✅ **关键词**（§3）· 命中片段 · 状态条 · Result→Note 落点 · 标签 chip |
| 设置 | ✅ 两层。**「智能搜索」那一段在纯词法生产下不出现**（§9.3） |
| 权限被拒 | ✅ 麦克风有完整 UX（解释 · 打开设置 · 取消 · 回前台自动重试）。文案与「给不给去设置按钮」的判断在内核，有断言守着 |
| 总结生成时机 | ✅ 统一到「退出笔记页」一条规则。点进去看一眼不会花钱 |
| **删除笔记** | ✅ **本轮修了一个真机 crash**（§9.4）。模拟器上不复现 |
| **新建笔记** | ✅ **什么都不写就离开 → 不留下任何东西**（§9.5）。空白字符不算内容；只有图片 / 录音 / 文档 / 链接的笔记照常保留 |
| **设置页键盘** | ✅ **输入完成后键盘会收起**（§9.6）。三条退出路径：键盘「完成」· Return · 滑动列表 |
| iCloud 同步 | ⛔ **此版本不提供**，据实显示；不给必然失败的开关 |
| 语义弃答 | ⛔ **EXPERIMENTAL，未接入生产**（TD-10 余量 0.0002），有断言守着 |
| PDF 导出 · 媒体文件同步 | ⛔ 未实现 |

---

## 3. 检索 —— 生产是 keyword

```
PRODUCTION_RETRIEVAL   = KEYWORD          ✅ 生产
LOCAL_HYBRID           = EXPERIMENTAL     🧪 只在 Developer Tools / MosaicBench
CLOUD_SEMANTIC         = DEFERRED         ⏸ 产品决策，不是缺凭据
ABSTENTION             = EXPERIMENTAL     🧪 未接生产
```

### 为什么是 keyword

真机实测（iPhone Air / iOS 27.0 / Release / scenario-v5 holdout）：

| arm | R@1 | R@3 | R@5 | MRR | P50 | P95 | 负例克制率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **keyword** | **0.604** | **0.654** | **0.673** | 0.625 | **2.29–2.36 ms** | **8.67–13.14 ms** | **100.0%** |
| local-hybrid | 0.604 | 0.635 | 0.673 | 0.627 | 8.96–9.48 ms | 16.03–16.75 ms | 0.0% |

质量数字两次跑批**逐位相同**；延迟给区间。

三条各自独立的理由：

1. **质量增益 ≈ 0。** R@1 / R@5 的 delta 是精确的 `+0.000`（CI `[0,0]`），
   MRR `+0.002` 区间跨过 0。**R@3 反而更低。**
2. **延迟代价确定：P50 约 +300%**（2.3 → 9.2 ms，两次跑批一致）。两者都远在
   250 ms 天花板内，但天花板之内不等于免费。
3. **负例克制率 100% → 0%。** keyword 对「本来就没有答案」的 query 如实返回空；
   local-hybrid 把整个语料按余弦排下来，**每一条无答案的 query 都会拿到一屏结果**
   （TD-10：向量路没有相关性下限）。这一条 Gate 不看，却是三条里最严重的。

> **Gate = PASS，Promotion = NO。** 这是合法状态，说明见 `GATE_POLICY.md` §5b。

### 其余

| | 状态 |
|---|---|
| CJK query 切分 | ✅ 二元组 + `minimum_should_match`。修复前中文长 query 在词法路全灭 |
| 拉丁词 `minimum_should_match` | ✅ 三个以上词时命中一半即可 |
| 标题/标签命中的判定 | ✅ 它是字面命中，不再谎称「没有完全匹配的关键词」 |
| 归一化缓存 | ✅ **本轮才真正到达用户**（§9.2）。未命中批次并行折叠 |
| derived 数据一致性 | ✅ 删文件夹会清理 · 启动清历史孤儿 · 陈旧项不占 topK。**本轮加了真机 UI 端到端**（Core Flow 6） |
| 索引 | ✅ 启动灌回 + 增量 + 防抖 + StaleGuard。**发布构建不建向量索引**（§9.1） |
| 云端 provider | ✅ 代码在，标记 `DEFERRED`；缺凭据判 `SKIPPED — CREDENTIALS NOT CONFIGURED`，**不构成 FAIL** |
| 跨语言 | ⚠️ 已知边界。本地双索引实测**比 baseline 更差**，只有云端能解 |
| ANN / 向量库 | ⛔ 明确不做 |

---

## 4. 评测与判定

| | 状态 |
|---|---|
| 评测集 | ✅ `scenario-v5`：242 篇语料 / **207 条 query** / 九类能力 / 分级相关性 / 硬负例 |
| Development / Holdout | ✅ 144 / 63，按类别分层，确定性划分，**带 sha256 冻结指纹** |
| Holdout | ✅ **冻结，本轮没有在它上面调过任何参数** |
| 回归集口径 | ✅ **棘轮**：只放当前通过的用例（7 条，覆盖 4 类）；仍失败的进 backlog |
| 数据来源标记 | ✅ `agent_authored_realistic` 写进每一条，并有断言守着 |
| **`human_authored` 导入口** | ✅ **本轮打通**：schema 接受它，`notesVisibleWhileAuthoring` 也留好了。**没有制造任何假的人工数据** |
| 发布判定 | ✅ `gate-v1` = baseline 相对 + 统计置信（配对 bootstrap）+ 绝对下限 + 延迟 + 回归 |
| Gate 下限 | ⚠️ **PROVISIONAL**（`GATE_POLICY_VERSION = V1_PROVISIONAL`）。依据是当前这份 agent 编写的评测集，拿到人工评测集后**必须重新校准** |
| 本轮结论 | ✅ Mac 上 development / holdout 双双 PASS · **真机上 holdout 也 PASS** |
| 云端臂 | ⏸ `SKIPPED — CREDENTIALS NOT CONFIGURED`（DEFERRED，不是失败） |

---

## 5. 测试与构建 —— 三种机器分开报

**「全绿」必须带上「在什么条件下」。**

### Mac / CLI

| 套件 | 结果 | 条件 |
|---|---|---|
| `swift run mosaic-checks` | ✅ **3714 断言** | Mac · debug · 约 7 分钟 · **无云端凭据** |

### 模拟器（iPhone 17 Pro · iOS 26.5）

| 套件 | 结果 |
|---|---|
| `MosaicTests` | ✅ **107 条** |
| `MosaicUITests` | ✅ **12 条** |
| Release 构建 | ✅ |
| `tools/verify_release.sh` | ✅ 全过 |
| Debug / Release 构建警告 | ✅ **0** |

### 真实 iPhone（iPhone Air · iPhone18,4 · iOS 27.0 · arm64）

| 套件 | 结果 | 条件 |
|---|---|---|
| Debug 真机构建 | ✅ | 个人 Team + `-allowProvisioningUpdates` |
| Release 真机构建 | ✅ | 同上 |
| `MosaicTests` | ✅ **107 条 · 0 skip** | Debug |
| `MosaicUITests` | ✅ **12 条** | Debug · 需要设备开「UI 自动化」 |
| `MosaicBench` | ✅ **8 条 · 7 过 · 1 skip** | **Release**。skip 的是云端臂（DEFERRED） |
| `tools/verify_device.sh` | ✅ 全过 | 验的是 **arm64 Release 切片** |

> **`MosaicBench` 此前是「7 条在模拟器上按设计 skip」。** 本轮真机跑起来了，
> 而且第一次跑就抓到一条 FAIL（§9.2）。

---

## 6. 真机基准（权威数字 · 不与 Mac 混表）

设备 `iPhone Air (iPhone18,4)` · iOS `27.0.0` · arm64 · **Release** ·
thermal `nominal` · 低电量模式关闭 · 取样量 25。

### Metric A · keyword 生产路径

**两次独立跑批的区间。** 单个数字会把精度说得太满 —— 最大档上两次差了 9.5 ms。

| chunks | 冷 P50 | 冷 P95 | 热 P50 | 热 P95 |
|---:|---:|---:|---:|---:|
| 1 000 | 4.60 – 4.72 ms | 11.18 – 12.01 ms | 2.96 – 3.22 ms | 4.36 – 4.67 ms |
| 5 000 | 19.59 – 19.67 ms | 27.93 – 29.39 ms | 15.56 – 15.80 ms | 22.68 – 23.69 ms |
| 10 000 | 38.63 – 38.95 ms | 58.50 – 58.71 ms | 33.69 – 34.19 ms | 48.48 – 51.02 ms |
| 20 000 | **81.54 – 90.99 ms** | **118.95 – 124.13 ms** | 67.12 – 74.79 ms | 102.20 – 112.78 ms |

预算冷热两侧都是 P50 < 100 ms / P95 < 250 ms。**全部通过。**

⚠️ **余量要说准**：最大档冷 P50 的预算余量只有 **10–20%**，而两次跑批之间就摆动 9.5 ms。
P95 那一侧余量 2.0×，是舒服的；P50 这一侧不是。
20k 是 PRD 的上限档 —— 1k 上冷 P50 只有 4.6 ms。**下一次动词法路时要重测这一格。**

### Metric B-local · hybrid 端到端（实验臂）

| chunks | P50 | P95 |
|---:|---:|---:|
| 1 000 | 7.66 – 7.78 ms | 11.02 – 11.48 ms |
| 5 000 | 22.34 – 22.50 ms | 28.89 – 30.83 ms |
| 20 000 | 82.39 – 83.66 ms | 112.65 – 117.09 ms |

L2a query 嵌入 P50 4.35 – 4.39 ms（占同批次 20k 端到端 5.3%）。
**绝对值不可单独引用**（历史三次读数 6.21 / 11.1 / 4.3，机制未查清）。

### 其他

| 项 | 真机实测 |
|---|---|
| 本机句向量模型 | zh-Hans ✅ 640 维 · en ✅ 512 维（模拟器上 zh-Hans 不可用 —— TD-9 到此关闭） |
| 余弦检索（20k / dim 384） | P50 5.47 – 5.52 ms · P95 5.95 – 6.04 ms · 常驻 29.3 MB |
| 建索引（278 chunks · 本机模型） | 4 990 – 5 444 ms（18.0 – 19.6 ms/chunk） |
| 五个已修缺陷的证人 | **7/7 全部 rank 1**，跑在生产 keyword 配置上 |
| 云端臂 | ⏸ `SKIPPED` |

---

## 7. 发布硬化

| | 状态 |
|---|---|
| App 图标 | ✅ 已编译进**真机 Release** 的 `Assets.car`（脚本核对）。**临时品牌资产，可在上架前替换** |
| 隐私清单 | ✅ 逐项 grep 核对后写出，只声明用到的两类 Required Reason API；已打进真机包 |
| 权限说明 | ✅ 三条都对应真实用途；删掉了用不到的相册权限 |
| Release 排除内部工具 | ✅ 五个类 0 符号、「开发者工具」0 字符串 —— **模拟器切片与真机 arm64 切片各验一次** |
| 生产配置进包 | ✅ 真机包里能 `strings` 到 `retrieval-v2-keyword` |
| 第三方 SDK | ✅ 没有 |
| 签名 | ✅ 个人 Team 可出 Debug / Release 真机构建 |

---

## 8. 技术债

| ID | 状态 |
|---|---|
| **TD-1** | `SummaryService` 无 stale 保护（既有缺陷）。**开放**，一直没动 |
| **TD-4** | `AIJobCoordinator` 排队取消会漏 waiter。**开放**，当前深度无害 |
| **TD-6** | 向量索引仅内存。**开放**，且在纯词法生产下发布构建根本不建它 |
| **TD-10** | 相关性下限。**未解决**，余量 0.0002。它是 local-hybrid 不能上生产的直接原因（§3 第 3 条） |
| **TD-11** | 余弦受长度支配。**provider-specific** |
| ~~TD-9~~ | **本轮关闭**：真机上 zh-Hans 与 en 都可用，640 / 512 维 |
| ~~TD-2/3/5/7/8/12~~ | 已关闭 |

---

## 9. 本轮抓到的六件事

前四条来自**真机验证**，没有一条能在模拟器上发现。
后两条（§9.5 / §9.6）是收口前用户实际使用时提出的 —— 它们不需要真机才能发现，
只需要**有人真的把 App 当 App 用一遍**。这本身是个结论：
自动化测试覆盖的是「功能做没做对」，覆盖不了「用起来是不是这么回事」。

### 9.1 文档说 keyword，代码默认 hybrid

`RetrievalConfig.production` 写作 `RetrievalConfig()`，而初始化器的 `mode`
默认值是 `.hybrid`。文档 / Gate baseline / 真机基准全都说 keyword，**线上跑的是 hybrid**。
两条路都能跑通，所以没有一条既有断言会因此变红。

修法不只是改常量：`mode` 的默认值删掉了（每个构造点必须写出走哪条路），
落盘的注册表加了迁移（老设备上那条根记录还是 hybrid），
新增 `ProductionConfigChecks` 把「生产是什么」写成断言。

### 9.2 归一化缓存从来没有到达过用户

真机 Metric A 冷 P50 在 20k 上 **103.01 ms**，超了 100 ms 预算。查下去两件事：

- 生产搜索**每次按键都新建一个 `RetrievalService`**，它的 `NormalizedTextCache`
  每次都是空的 —— 线上永远走冷路径。缓存实现了、单测守着、基准测过，唯独没接上。
  **两条路结果逐位相同，所以没有任何既有测试会变红。**
- 冷路径里 35.4 ms 花在归一化上，而每个 chunk 的折叠彼此无依赖 —— 天然可并行却在串行跑。

改完 103.01 → **81.5–91.0 ms**（两次跑批）。仍然过线，但 20k 上的余量只剩 10–20%。

### 9.3 一个开了也没用的开关

生产是 keyword 之后，高级设置里的「智能搜索」那一段（Embedding 模型 / 维度 /
Base URL / 云端同意开关）对搜索行为**没有任何影响**。留着它比留一个 iCloud 假开关更糟 ——
它还要求用户授权一次「把笔记文字发到第三方」，换来的却是零变化。
现在这一段在纯词法生产下不出现，据实显示「搜索方式：关键词」。

搜索页的状态栏同理：不再说「智能搜索暂不可用」。**没承诺过的能力，不存在「不可用」。**

### 9.4 删除笔记会让 App 崩掉

在笔记页删掉笔记 → App 当场退出。栈是 `Card.tags.getter ←
NoteDetailView.blockList ← body`，底下是 SwiftData 的 `_assertionFailure`。
**SwiftData 里删掉的对象不能再读属性。**

根因是时序：`dismiss()` 不同步生效，中间那一帧 body 还在读一个已删除的对象。
修的过程中错了两次，两次都写进注释了：守卫放在 `body` 顶层挡不住
（SwiftUI 会直接重算已建好的内容闭包）；`card.isDeleted` **不是**一直为真
（`delete()` 后为真，`save()` 后变假 —— 要配 `modelContext == nil` 才完整）。

---

### 9.5 新建笔记会留下「未命名笔记」（收口期发现）

> **Late Closure Finding: new-note creation persisted empty drafts as untitled notes.**

点「新建」立刻 `insert` + `save` 一条 `Card`，用户什么都不写就返回 ——
笔记流里留下一行「未命名笔记」。每一次误触悬浮按钮都会产生一条，
而清理它要用户自己去长按删除：**一个由 App 制造、却要用户收拾的烂摊子。**

产品语义改成：点「新建」只表达一次**创建意图**，不等于数据库里已经有了一条笔记。

判定在内核（`NoteDraftPolicy`），因为它必须**只有一处实现** ——
两处迟早会在「图片算不算内容」这种问题上分叉，而分叉的后果是删掉用户的照片。
有效内容 = 标题 / 标签 / 任意非空内容块（文字 · 图片 · 录音 · 文档 · 链接）/
OCR 与转写文本，任一即可；空白字符 trim 后判空。

三处收口：

1. **退出笔记页时丢弃**（`NoteDetailView.handleExit`）。走的是删除笔记那条**既有**
   清理路径（媒体文件 → Card → derived），不是新写一遍 ——
   重写一遍就等于再制造一次 orphan derived data（上一轮 D1 修的正是这个）。
2. **只管新建，不管已有笔记。** `isNewDraft` 是必需的第二个条件：
   用户把一条老笔记清空再退出，**不能**替他删掉。「清空」和「删除」是两个意图。
3. **启动时清幽灵**（`EmptyDraftSweep`）。App 在空笔记里被划掉时 `onDisappear`
   跑不到，会留下一条。判据用的是同一个内核规则，所以被清掉的只可能是
   一条用户看不到任何内容的笔记。

另有一处防御：`onDisappear` **不等于**「这一屏走了」——
往上推一屏（笔记页 →「设置」）同样会触发它。摘要生成走这条路只是白花一次钱，
**删除走这条路是丢数据**，所以显式挡住了。

### 9.6 设置页输入完成后键盘不收起（收口期发现）

> **Late Closure Finding: settings keyboard focus was not dismissed after text entry.**

设置里的每一个输入框（Base URL / 模型名 / API Key / STT / Embedding 维度）
输入完成后键盘都留在屏幕上。表单下面还有 Toggle 和按钮，
于是用户要么盲点被挡住的区域，要么退出这一屏 —— 两种都不是「完成输入」。

修法是一个可复用的 `settingsKeyboardDismissal(focus:)`，基于 `@FocusState`，
**不用 `UIApplication.endEditing`** —— 那是一句绕过 SwiftUI 焦点系统的全局命令，
会让 `@FocusState` 与真实焦点不一致，而且不可组合（每屏复制一遍，下一个新增的
输入框一定会漏）。

三条退出路径对应三种**不同的用户意图**，缺一条就会在某个场景卡住：

| 路径 | 靠什么 | 覆盖 |
|---|---|---|
| 键盘上的「完成」 | `ToolbarItemGroup(placement: .keyboard)` | **全部**，含 `numberPad` 这种没有 Return 键的 |
| 软键盘 Return | `.submitLabel(.done)` + `.onSubmit` | 单行文本 / URL / 密码 |
| 滑动列表 | `.scrollDismissesKeyboard(.interactively)` | 想看被挡住的内容时的自然动作 |

**收起键盘 ≠ 取消输入**：设置项仍然是即时保存，这个修饰符不碰数据。
真机用例断言的是 `app.keyboards` 真的消失，**不是**某个状态变量为 nil ——
用 App 自己说的话去验证 App 自己的行为等于什么都没验。

---

## 10. 上一轮被数据推翻的结论（不要重新捡起来）

| 曾经以为 | 实测 |
|---|---|
| 中英双索引能救跨语言 | **比 baseline 更差**（0.158 vs 0.211） |
| RRF 至少是中性的 | 本地臂上**主动有害**（R@1 0.343 → 0.269） |
| keyword 慢是因为没有倒排索引 | 68.5% 花在与 query 无关的**归一化重算**上 |
| 原生 `String.range(of:options:)` 更快 | **更慢**（91/242 ms vs 63 ms） |
| iOS 上没有中文句向量模型 | 真机上**有**（640 维）—— 模拟器的锅 |
| cross-language R@1 能靠调 fusion 改善 | 摆动 **0.000** |
| 「keyword 对无答案 query 100% 克制」是质量保证 | 那是**分词缺陷的副产品** |
| 自然语言中文 query 在词法路零命中是设计前提 | 那是缺陷。修好后 R@1 0.175 → 0.478 |
| 「多一个词也没关系，AND 只是更严格」 | 拉丁 AND 让噪声类长期 0.000 |
| 回归集应该收录「该修的用例」 | 那会让 Gate 永久红而被忽略。回归集是**棘轮** |
| **归一化缓存已经在生产生效** | **从来没有** —— 每次按键都新建一个空的（§9.2） |
| **`card.isDeleted` 在删除后一直为真** | `save()` 之后**变回假**（§9.4） |
| **模拟器上全绿就够了** | 真机第一轮跑出 1 条性能 FAIL + 1 个 crash + 14 条只在模拟器成立的测试 |
