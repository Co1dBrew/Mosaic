# Mosaic · 项目现状

> **这份文件回答「现在到底是什么状态」。** 计划与历史在别处（`design/`、`HANDOFF.md`）。
> 一份同时描述「打算怎么做」和「现在怎么样」的文档一定会过期，所以这两件事分开写。
>
> 最后更新：2026-08-29

---

## 0. 一句话

产品（UI v2）与检索质量平台都已落地并有测试守着；
**发布判定 development / holdout 双双 PASS**，但 PASS 的是 **baseline（keyword）**——
本机语义路对 R@1 的贡献是精确的 0，却要多付 3.7× 延迟。
按现在的数据**该发的是 keyword**，而不是候选的 local-hybrid；
语义路要成立得靠云端臂，而云端臂缺凭据没跑。

---

## 1. 发布就绪度

| 目标 | 结论 | 理由 |
|---|---|---|
| **Internal Beta**（内部装机试用） | ✅ **可以** | 构建干净、核心流程有 UI 测试守着、内部工具可用 |
| **External Beta / TestFlight** | ⚠️ **技术上可以，产品上不建议** | 发布必需项都齐了（图标 / 隐私清单 / 权限 / 内部工具已排除），但见 B1–B3 |
| **App Store** | ❌ **不可以** | 评测集仍非真实用户数据，质量承诺没有依据 |

### 阻断项

| # | 阻断什么 | 谁能解 |
|---|---|---|
| **B1** | **本机语义路不值得发**：对 R@1 贡献 +0.000（CI [0,0]），却把 P50 从 13.6 ms 推到 50.2 ms。要么只发 keyword，要么等云端臂 | 产品决策 + 用户凭据 |
| **B2** | **云端臂 NOT RUN**：缺 `MOSAIC_LIVE_EMBEDDING_*` 四个环境变量 | **只有用户能解** |
| **B3** | **真机延迟未在本轮复测**：词法路 P95 从 2.9 ms 涨到 26–30 ms（Mac debug），真机数字待验证 | **只有用户能解**（要连真机） |
| **B4** | **评测集不是真实用户数据**：`provenance = agent_authored_realistic` | **只有用户能解**（真实笔记 + 人工标注） |

B1 与 B2 是同一件事的两面：质量要从云端来。

---

## 2. 产品（Notes App）

| | 状态 |
|---|---|
| 信息架构 | ✅ UI v2：**2 级**（笔记流 → 笔记页）。旧的 3 级已删除 |
| 首页 | ✅ 文件夹 chip 筛选 · 笔记行（标题 + 一句话 + 时间 + 未读点）· 滑动与长按 · 悬浮新建 |
| 笔记页 | ✅ 摘要条（44pt，原地展开）· 文件夹选择器 · `⋯` 收口 · 底部插入工具条 |
| 内容块 | ✅ 文字（零按钮，聚焦即源码）· 图片 · 录音（转写直显）· 文档 · 链接 |
| 新增功能 | ✅ **移动到文件夹** · **文件夹拖动排序**（PRD 标 P1，此前都没实现） |
| 搜索 | ✅ 隐式混合 · 命中片段 · 状态条 · Result→Note 落点 · 标签 chip |
| 设置 | ✅ 两层（第一屏只放需要决策的项；自定义服务商时 Base URL / 模型名提升回第一屏） |
| 权限被拒 | ✅ 麦克风有完整 UX（解释 · 打开设置 · 取消 · 回前台自动重试） |
| 总结生成时机 | ✅ 统一到「退出笔记页」一条规则。**点进去看一眼不会花钱** |
| iCloud 同步 | ⛔ **此版本不提供**，据实显示；不给必然失败的开关 |
| 语义弃答 | ⛔ **EXPERIMENTAL，未接入生产**（TD-10 余量 0.0002），有断言守着 |
| PDF 导出 | ⛔ 未实现（有 Markdown / 纯文本） |
| 媒体文件同步 | ⛔ 未实现 |

---

## 3. 检索

| | 状态 |
|---|---|
| 三路检索 + 融合 | ✅ 默认 `weighted(keyword: 0.7, vector: 0.3)`（RRF 实测在本地臂**有害**） |
| CJK query 切分 | ✅ **本轮新修**：二元组 + `minimum_should_match`。修复前中文长 query 在词法路全灭 |
| 拉丁词 `minimum_should_match` | ✅ **本轮新修**：三个以上词时命中一半即可。修复前多一个词就整条不命中 |
| 检索质量（Mac · 本机模型） | R@1 **0.175 → 0.478**（development）· **0.173 → 0.604**（holdout） |
| 归一化缓存 | ✅ 2.36×（真机 2.29–2.30×），语义逐位不变 |
| derived 数据一致性 | ✅ **本轮新修**：删文件夹会清理 · 启动清历史孤儿 · 陈旧项不占 topK 名额 |
| 索引 | ✅ 启动灌回 + 增量 + 防抖 + StaleGuard |
| 云端 provider | ✅ 已实现，路由云端优先；**同意闸门默认关闭** |
| 跨语言 | ⚠️ 已知边界。本地双索引实测**比 baseline 更差**，只有云端能解 |
| ANN / 向量库 | ⛔ 明确不做（20k 上余弦 P95 5.9 ms，瓶颈在词法路不在向量路） |

---

## 4. 评测与判定

| | 状态 |
|---|---|
| 评测集 | ✅ `scenario-v5`：242 篇语料 / **207 条 query** / 九类能力 / 分级相关性 / 硬负例 |
| Development / Holdout | ✅ 144 / 63，按类别分层，确定性划分，**带 sha256 冻结指纹** |
| 回归集口径 | ✅ **棘轮**：只放当前通过的用例；仍失败的进 backlog（`GATE_POLICY.md` §7） |
| 数据来源标记 | ✅ `agent_authored_realistic` 写进每一条，并有断言守着 |
| 质量自检 | ✅ 生成时（Python）+ 每次跑 checks（Swift）各一遍 |
| 发布判定 | ✅ `gate-v1` = baseline 相对 + **统计置信**（配对 bootstrap）+ **绝对下限** |
| 本轮结论 | ✅ development / holdout 双双 **PASS**（四项全绿）—— 但 PASS 的是 baseline，见 `GATE_POLICY.md` §5 |
| 云端臂 | ⚠️ **NOT RUN — MISSING CREDENTIALS** |
| 回归集 | ✅ **7 条**，覆盖 4 个类别，每次跑批逐条点名并断言全过 |
| 真机基准 | ⚠️ 上一轮的数字仍然有效，但**本轮改动后未复测** |

---

## 5. 测试与构建

| 套件 | 结果 | 条件 |
|---|---|---|
| `swift run mosaic-checks` | ✅ **3637 断言** | Mac · debug · 约 4 分钟 · 无云端凭据 |
| `xcodebuild test -scheme Mosaic` | ✅ **93 单测 + 6 UI 测试** | iPhone 17 Pro 模拟器 |
| `MosaicBench` | ⏭ 7 条在模拟器上按设计 skip | 要真机 |
| `./tools/verify_release.sh` | ✅ 全部通过 | Release 产物核对 |
| Debug / Release 构建警告 | ✅ **0** | |

> **「全绿」必须带上「在什么条件下」。** 上面这批是**没有云端凭据**跑出来的：
> 云端那一段整段包在 `if let cloud…` 里，缺 key 就不执行，也就没在保护任何东西。

---

## 6. 发布硬化

| | 状态 |
|---|---|
| App 图标 | ✅ 有（**临时品牌资产**，脚本生成，等最终设计替换） |
| 隐私清单 | ✅ 逐项 grep 核对后写出，只声明用到的两类 Required Reason API |
| 权限说明 | ✅ 三条都对应真实用途；**删掉了用不到的相册权限** |
| Release 排除内部工具 | ✅ 五个类 0 符号、「开发者工具」0 字符串（脚本核对） |
| 第三方 SDK | ✅ 没有 |
| 签名 / 描述文件 | ⚠️ 真机构建需要 `-allowProvisioningUpdates`（个人 Team） |

---

## 7. 技术债

| ID | 状态 |
|---|---|
| **TD-1** | `SummaryService` 无 stale 保护（既有缺陷）。**开放**，一直没动 |
| **TD-4** | `AIJobCoordinator` 排队取消会漏 waiter。**开放**，当前深度无害 |
| **TD-6** | 向量索引仅内存，启动从 derived store 重建。**开放**（20k ≈ 29.3 MB，真机可接受） |
| **TD-10** | 相关性下限。**未解决**，余量 0.0002 不可发布。默认 `neverAbstains`，有断言守着不进生产 |
| **TD-11** | 余弦受长度支配。**provider-specific**，不是「系统永久解决」 |
| ~~TD-9 / TD-12~~ | 已关闭 |
| ~~TD-2/3/5/7/8~~ | 已关闭 |

---

## 8. 上一轮被数据推翻的结论（不要重新捡起来）

| 曾经以为 | 实测 |
|---|---|
| 中英双索引能救跨语言 | **比 baseline 更差**（0.158 vs 0.211） |
| RRF 至少是中性的 | 本地臂上**主动有害**（R@1 0.343 → 0.269） |
| keyword 慢是因为没有倒排索引 | 68.5% 花在与 query 无关的**归一化重算**上 |
| 原生 `String.range(of:options:)` 更快 | **更慢**（91/242 ms vs 63 ms） |
| iOS 上没有中文句向量模型 | 真机上**有**（模拟器的锅） |
| cross-language R@1 能靠调 fusion 改善 | 摆动 **0.000** |
| 「keyword 对无答案 query 100% 克制」是质量保证 | 那是**分词缺陷的副产品**（查不动中文当然不返回） |
| 自然语言中文 query 在词法路零命中是设计前提 | 那是缺陷。修好后 R@1 0.175 → 0.478 |
| 「多一个词也没关系，AND 只是更严格」 | 拉丁 AND 让 `wheres the midterm now…` **整条不命中**，噪声类长期 0.000 |
| 回归集应该收录「该修的用例」 | 那会让 Gate 永久红而被忽略。回归集是**棘轮**，只收当前通过的 |
